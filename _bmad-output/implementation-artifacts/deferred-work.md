# Deferred Work

## ✅ Epic 8 Gate 0 — MonsterDatabase + scenes authored, Bootstrap seams closed (2026-06-17)

The Editor-authoring session landed (James in Editor) + the headless seam closure (this session). **Closes** the long-standing `MonsterDatabase.asset` blocker (Story 2.2 Task-3) and the 4.1–4.6 + 2.3 "no live EncounterService/CodexService registration" deferrals (the registration CODE — runtime resolution stays a device claim). What landed:

- **Assets (Editor):** 5 MVP monster `.asset` files (`Mon01`–`Mon05`, `Mon03_VeilCrawler` = Rare), `MonsterDatabase.asset` populated (count 5), 5 placeholder Art sprites, the 5 game scenes (`Onboarding`/`Home`/`ARHunt`/`Codex`/`Shop`) + the six-slot build order, and the `_economyConfig` inspector assignment on `Bootstrap.unity`.
- **Seam code (headless):** `Bootstrap.cs` now constructs + registers `CodexService` + `EncounterService` (sharing the `economyMutationLock`) + wires the `AppStateMachine` to the live encounter via two thin App-tier binders in the new `EncounterServiceAppAdapters.cs` (`SnapshotPortAdapter` : `IEncounterSnapshotPort`, `ShortfallAdapter` : `IInsufficientCreditsSource` — `EncounterService` sits below App in the graph so cannot implement App-tier interfaces directly, AR-5). Added `[SerializeField] MonsterDatabase _monsterDatabase` (null-checked, the `_economyConfig` precedent).
- **Tests (headless):** new on-disk `MonsterDatabaseAssetAuditTests` (5 pins, all passing); deleted the tautological `Asset_file_name_convention` test; un-ignored the `_economyConfig` build-settings pin (now passing); added the `_monsterDatabase` pin. **690 total / 689 passed / 0 failed / 1 ignored, 0 `error CS`.**

**UPDATE (2026-06-17, same day):** the remaining Editor drag is DONE — `MonsterDatabase.asset` assigned to the `_monsterDatabase` slot on the Bootstrap component, the `Bootstrap_scene_assigns_the_MonsterDatabase_reference` pin un-ignored + passing. Full EditMode gate now **690 passed / 0 failed / 0 ignored, 0 `error CS`** (no skips). **Gate 0 is COMPLETE** for all headless-provable work; the only Gate-0-related item left is the on-device "GameServices resolves CodexService/EncounterService post-boot" check, which is a Gate-5 device claim (not headless-provable). Next frontier: Gate 1 (AR rig + `#if UNITY_ANDROID` device bodies, Story 8.3 — device-bound).

## Deferred from: code review of 8-3-author-the-ar-rig-scene-contents-and-its-device-glue (2026-06-17)

Adversarial 3-layer CR (18 agents): 15 raw → 8 confirmed (8 patched) / 1 defer / 4 sanctioned / 2 false-positive; 1 AC violation (patched). **All confirmed findings were patched in-cycle — no forward debt deferred.** The headline catch: the scene-rig guard's original type-name `Contains` match was structurally un-greenable (AR Foundation managers serialize as `m_Script` guids with empty `m_EditorClassIdentifier`; the type name never appears in the YAML) AND substring-collision-prone (`"ARSession"` ⊂ `"ARSessionOrigin"`); the guard was rewritten to match by package script GUID (resolved at test time from the `.cs.meta`). The one auditor "defer" (fix the ARSession substring quirk before un-ignoring) was **subsumed by that patch** — fixed now at the root, so nothing is carried forward. Full triage in the story's `### Review Findings`. No items deferred to a future story.

## Deferred from: Story 8.3 — AR rig + device-glue authoring checklist (2026-06-17)

8.3 (author the AR rig scene contents + the three adapters' `#if UNITY_ANDROID` device bodies + the monster prefab) is a **DEVICE story** — even more device-weighted than 8.2. A recon confirmed the hard split: the three production adapters reference ZERO AR Foundation types (all real code lives inside `#if UNITY_ANDROID && !UNITY_EDITOR`, excluded from EditMode compile, CI-untestable by design — architecture.md:591); NO EditMode test touches them (they are proven through Fakes). So 8.3's verification gate is the on-device AR smoke (Gate 1 / Gate 5), NOT new EditMode tests.

The HEADLESS slice landed this session (story-automator, ungated): the `ArSceneRigGuardTests` scene-component-presence pin (`[Ignore]`-pending-Editor — it asserts all six AR managers + an XR Origin are present in `ARHunt.unity`, so it FAILS against today's placeholder rig; never green-but-false); the owner-tag reconciliation (every AR-device-body `TODO(Story 6.3)` retargeted to `(Story 8.3)`); and the AR regression floor re-confirmed green. The items below are the **device/Editor authoring checklist** — owner: an Editor-authoring session + a Galaxy S21+ (the Story 8.7 release gate). NOT auto-claimed done. Sequenced: add the package refs → author the rig → un-ignore the pin → implement the device bodies → author the prefab → on-device smoke. Full detail in `docs/epic-8-device-release-gate.md#Gate-1`.

- **[8.3-checklist] Add the AR-Foundation package refs to `Veilwalkers.AR.asmdef`** (`Unity.XR.ARFoundation`, `Unity.XR.CoreUtils`, `Unity.XR.ARSubsystems`, + `Unity.XR.ARCore` and the ARCore Extensions assembly IF cloud/persistent anchors are used for `TryReacquireAnchor` — D2 RECOMMEND trackable re-acquire within a session first; defer cloud anchors). These are Unity package edges, NOT `Veilwalkers.*`, so `AcyclicDependencyTests` stays green. Owner: Editor session. [Assets/Veilwalkers/AR/Veilwalkers.AR.asmdef]
- **[8.3-checklist] Author the AR rig into `ARHunt.unity`** (AC-1): XR Origin (`XROrigin`/`ARSessionOrigin`) + AR camera + camera-background + the six managers (`ARSession`, `ARPlaneManager`, `ARAnchorManager`, `ARRaycastManager`, `AROcclusionManager`, `ARCameraManager`) + ARCore loader enabled. A correct rig (valid component fileIDs/guids) is Editor work, unsafe to hand-author as YAML; only verifies on device. **Then un-ignore `ArSceneRigGuardTests` + confirm green (mutation-check: removing any one rig component → RED).** The guard matches by package `m_Script` GUID (resolved from the AR Foundation `.cs.meta` — type names are not serialized into the scene), so the AR Foundation packages must be resolved on disk first; it fails loudly otherwise. Owner: Editor session. [Assets/Veilwalkers/Scenes/ARHunt.unity; Assets/Tests/EditMode/Architecture.Tests/ArSceneRigGuardTests.cs]
- **[8.3-checklist] Implement the three `#if UNITY_ANDROID && !UNITY_EDITOR` device bodies** (AC-2..AC-6): `ArcoreSession` (`IsSupported`→`ARSession.state`; `StartAsync`→enable + await tracking-ready; `Pause`/`Resume`→`ARSession.enabled`; `Stop`→`Reset()`/disable + **cancel an in-flight `StartAsync`**); `ArcoreAnchorProvider` forward (`ARPlaneManager`/`ARRaycastManager`/`ARAnchorManager` place + anchor + `AnchorToken` build) + restore (`TrackableId` re-acquire + frustum-projected `PlaneCandidate[]` with camera-to-plane distance); `GameObjectSpawnSink` (`Object.Instantiate`/`SetActive`/`SetPositionAndRotation` against the URP prefab pool under the session origin). Resolve scene managers LAZILY (Bootstrap ctors run before the rig is live). Leave the editor `#else` stubs byte-identical. Owner: Editor session. [Assets/Veilwalkers/AR/ArcoreSession.cs; ArcoreAnchorProvider.cs; GameObjectSpawnSink.cs]
- **[8.3-checklist] Author the monster prefab(s) + the URP pool** (AC-5/AC-6): D3 RECOMMEND a lit placeholder primitive for the first device smoke (the materialization VFX + final art are Story 8.5); occlusion (AROcclusionManager) + environmental lighting (ARCameraManager); the Nightveil-Filter shader hook reserved for 4.6/8.5. Owner: Editor session. [Assets/Veilwalkers/... (new prefab)]
- **[8.3-checklist / Gate 1 + Gate 5] On-device AR smoke (the verification gate)** on the Galaxy S21+: plane detect → place → anchor → occlusion + lighting → re-anchor moves the GameObject → pooled spawn under the cap; AR recovery Restored/RelocatedToPlane/Failed. Record results in `docs/epic-8-device-release-gate.md`. NEVER auto-claimed done from a headless session. Owner: device session (Story 8.7 release gate). [Galaxy S21+ + USB build-and-run]

## ⮕ RENDER / SCENE / DEVICE-BUILD class re-pointed to Epic 8 (2026-06-16 — retro Action Items #2 + #5)

Every deferred entry below whose owner was stamped **"Epic 6"**, **"Story 6.3"**, **"asset-authoring"**, **"the AR-rig scene"**, **"the device build"**, or **"the IAP-import session"** is part of the single **render / scene / MonoBehaviour-view / AR-rig / device-build** class that Epics 2–6 deferred uniformly. The Epic-6 retrospective (2026-06-16) created **Epic 8: Render, Scene & Device-Build Pass** to own this entire class so no entry names an already-closed story/epic as its future owner. The authoritative owner mapping (see `docs/epics.md#Epic-8`):

| Old owner stamp (now closed/superseded) | New Epic 8 owner |
|---|---|
| AR-21 / Play-submission blockers (applicationId, target SDK, .aab, EDM4U, signing) | **Story 8.1** |
| `SampleScene` removal (line ~347, was "Story 6.3"); the `.unity` scene list + build order; the Bootstrap `_economyConfig` on-disk assignment; the CodexService/EncounterService Bootstrap registration seams | **Story 8.2** |
| AR-rig scene + the `#if UNITY_ANDROID` device bodies (`ArcoreSession`/`ArcoreAnchorProvider`/`GameObjectSpawnSink`); occlusion/lighting render; pooled spawn on device; re-anchor GameObject move | **Story 8.3** |
| Thin `*View` MonoBehaviour binding + AppStateMachine-driven scene load/unload; `ArSessionView` `OnApplicationPause` pump (line ~228, ~254); prewarm trigger; daily-reward affordance; snapshot/rehydrate re-anchor render | **Story 8.4** |
| The chunky Pumpkin Patch render layer (materialization/Lure tween, 3-state Codex reveal, state-treatment renders, pack cards, non-blocking top-up sheet, wordmark, sprites/fonts); the Guaranteed-Rare Lure use-button (line ~78); `UnityIapStoreAdapter` device bodies (`PurchaseAsync`/`AcknowledgeAsync`) | **Story 8.5** |
| The accessibility RENDER floor (rendered ≥48dp bounds, dynamic-type reflow, reduced-motion visual taming, TalkBack/`AccessibilityManager.announce` binding + live-event subscribe/unsubscribe) | **Story 8.6** |
| The signed `.aab` build + on-device AR smoke + NFR-1/NFR-5 device budgets + Firebase Test Lab release gate | **Story 8.7** |

**This header re-points ownership for the whole class** — the individual entries below retain their original wording (the diagnostic detail is still useful) but their owner is now the Epic 8 story above, not the closed story they name. NON-render deferrals (the `AcyclicDependencyTests` IO-hardening robustness class, the Economy overflow-hardening class, the Phase-2 server-verifier class, the Billing concurrency-hardening class) are **not** Epic 8 — they keep their own forward owners.

## Deferred from: Story 8.2 — Editor/device authoring checklist (2026-06-16)

8.2 (author the game scenes + build order + close the Bootstrap registration seams) was re-scoped during CS: a recon proved most of it is **Editor/device-bound, not headless** (James's decision: "headless slice now, rest to checklist"). The HEADLESS slice landed (SampleScene de-registered from the build; `BuildSettingsGuardTests` pins SampleScene-removed + every-enabled-path-real + Bootstrap@0; an `[Ignore]`d EconomyConfig pin; the LoadPhase regression confirmed — 68x/68x EditMode green). The items below are the **Editor/device-authoring checklist** — owner: an Editor-authoring session (and the Story 8.7 release gate). They are NOT auto-claimed done; each names its blocker chain. Sequenced: author scenes → register the 6-slot order → assign EconomyConfig → author MonsterDatabase.asset → close the seams.

- **[8.2-checklist] Author the 5 `.unity` game scenes (Onboarding, Home, AR Hunt placeholder, Codex, Shop).** A `.unity` is YAML, but a correct scene (GameObject/Canvas hierarchies, serialized component refs with valid fileIDs/guids) is error-prone to hand-author and only fails when a device opens it — Editor work. The AR Hunt scene is a minimal placeholder here; Story 8.3 authors its AR-rig contents. Owner: Editor-authoring session. [Assets/Veilwalkers/Scenes/*.unity (new)]
- **[8.2-checklist] Register the 5 scenes into the six-slot build order** `Bootstrap(0) → Onboarding(1) → Home(2) → AR Hunt(3) → Codex(4) → Shop(5)`, each `enabled:1` — only AFTER they exist on disk (the `Every_enabled_scene_resolves_to_a_real_unity_asset` guard correctly fails on a phantom path). The guard does not need editing; it asserts "every enabled path is real" as scenes are added. Owner: Editor-authoring session (after the scenes). [ProjectSettings/EditorBuildSettings.asset]
- **[8.2-checklist] Assign `_economyConfig` on the Bootstrap component in `Bootstrap.unity`.** The on-disk Bootstrap MonoBehaviour has NO `_economyConfig` serialized ref — a real latent boot-failure (`WireServices` requires it; an unassigned config fails fast at boot). An Editor inspector drag (unsafe to hand-author as scene YAML). Un-ignore `BuildSettingsGuardTests.Bootstrap_scene_assigns_the_EconomyConfig_reference` once assigned. Owner: Editor-authoring session. [Assets/Veilwalkers/Scenes/Bootstrap.unity]
- **[8.2-checklist / closes deferred-work.md:327] Author `MonsterDatabase.asset` + the 3–5 MVP monster `.asset` files** (incl. ≥1 Rare+ with real imported art) — the long-standing Story 2.2 Task-3 `[~]` deferral. Needs the Editor: a `Sprite Art` ref needs an imported texture; a null `Art` fails `Validate()`. Then add the on-disk audit (load via `AssetDatabase`, assert `Validate()` empty + filenames `<Id>_<Name>` + `PopulatedCount ∈ [3..5]`) and delete the old tautological `Asset_file_name_convention` test. Owner: Editor-authoring session. [Assets/Veilwalkers/ScriptableObjects/*.asset (new); Assets/Tests/EditMode/Monsters.Tests; Architecture.Tests/MonsterDatabaseLoreCountTests.cs]
- **[8.2-checklist] Close the CodexService + EncounterService Bootstrap registration seams.** Blocked on `MonsterDatabase.asset` (above). The `Bootstrap.cs` seam comments (~310–341) document the exact steps: add `using Veilwalkers.Monsters;` + `[SerializeField] MonsterDatabase _monsterDatabase`, construct `CodexService(saveService, _monsterDatabase, clock)` + register it; construct `LureSystem/CaptureSystem/SlaySystem/EncounterService` (passing the SHARED `economyMutationLock`) + register it; wire `EncounterService` as the 2nd `IInsufficientCreditsSource` + the real `IEncounterSnapshotPort` (replacing `NoEncounter`/null). The registration CODE is headless-compilable + `AcyclicDependencyTests` stays green; whether GameServices RESOLVES both post-boot is a runtime/device claim. This settles the 4.1–4.6 + 2.3 "no live EncounterService/CodexService registration (MonsterDatabase.asset blocker)" deferrals (deferred-work.md:187,199,211,225,240). Owner: Editor-authoring session (after the asset). [Assets/Veilwalkers/App/Bootstrap.cs]
- **[8.2-checklist] (optional cleanup) Delete the dead `Assets/Scenes/SampleScene.unity` asset** + its `.meta`. 8.2 only DE-REGISTERED it from the build (Decision A — minimal diff); physically deleting the asset is a later cleanup. Owner: any future cleanup pass. [Assets/Scenes/SampleScene.unity]

## Deferred from: code review of 8-1-own-ar-21-play-submission-blockers-and-build-config-as-data (2026-06-16)

8.1 is the **AR-21 build-config-as-data** story — the FIRST Epic-8 story (data-first / cheapest-blocker-first). It pins the Android build config as committed `ProjectSettings.asset` data (real applicationId `com.veilwalkers.app`, target SDK 35, IL2CPP/ARM64 regression-guarded), authors the editor-only `.aab` build entry point (`VeilwalkersBuilder.cs`), the build guide (`docs/build-guide.md` + CLAUDE.md pointer), and a non-vacuous on-disk guard (`BuildConfigGuardTests.cs`, 7 pins, mutation-tested). Adversarial 3-layer CR: **33 raw → 33 deduped → 5 confirmed (4 code patches — incl. a real SECURITY fix: signing passwords were persisting into the tracked `ProjectSettings.asset`; now applied transiently + scrubbed in `finally` — + 1 positive AC-verification), 10 defer, 13 sanctioned, 5 false-positive, 0 AC violations.** 681/681 EditMode green (674→681; +7 build-config pins). The 8 genuine defers below are ALL `VeilwalkersBuilder` device-build-robustness items: 8.1's headless scope is "the build script EXISTS + pins the `.aab` toggle as committed text" (the Epic-8 verifiability invariant), so the script's deep runtime robustness is exercised only when a real build runs — **owner: Story 8.7 (the device release gate that actually invokes `VeilwalkersBuilder.BuildAndroidAppBundle`).** They belong together as one build-hardening pass when the device build lands.

- **[CR-deferred] `VeilwalkersBuilder` output dir `Builds/Android` not created before `BuildPlayer` writes to it.** `BuildPipeline.BuildPlayer` may need the parent directory; create it (`Directory.CreateDirectory`) before the build (or confirm Unity creates it). Owner: Story 8.7. [Assets/Editor/VeilwalkersBuilder.cs (BuildAndroidAppBundle)]
- **[CR-deferred] No stale-scene-path guard.** The build reads enabled scenes from `EditorBuildSettings`; a stale/missing scene path would fail at build time. 8.2 owns the scene list; 8.7's build should validate each path resolves on disk before building. Owner: Story 8.7 (with 8.2's scene list). [Assets/Editor/VeilwalkersBuilder.cs]
- **[CR-deferred] Partial env-var presence builds unsigned without a clear error.** If SOME but not all `VEILWALKERS_KEYSTORE_*` vars are set, the build silently proceeds unsigned. A submission build should fail loudly on a partial signing config (all-or-nothing with a clear message). Owner: Story 8.7. [Assets/Editor/VeilwalkersBuilder.cs (ApplySigningFromEnvironment)]
- **[CR-deferred] Whitespace-only env-var values pass `IsNullOrEmpty` then fail cryptically at keystore load.** Trim + validate the four signing env vars (and ideally `File.Exists(keystorePath)`) before applying. Owner: Story 8.7. [Assets/Editor/VeilwalkersBuilder.cs (ApplySigningFromEnvironment)]
- **[CR-deferred] `keystorePath` file-existence not validated before the build.** A missing keystore file should fail with a clear pre-build message, not a cryptic Gradle error. Owner: Story 8.7. [Assets/Editor/VeilwalkersBuilder.cs (ApplySigningFromEnvironment)]
- **[CR-deferred — robustness class, one pass] The broader "`VeilwalkersBuilder` is a stub-for-text-guard, not yet hardened for a real build" class** (output dir, scene validation, signing all-or-nothing + file-existence + whitespace trimming, build-target-module-present check beyond the switch guard already added). Best fixed as one consistency pass when 8.7 first invokes the script on a build machine. Owner: Story 8.7. [Assets/Editor/VeilwalkersBuilder.cs]
- **[CR-deferred — note] The `.aab` `EditorUserBuildSettings.buildAppBundle` toggle is set at build time by the script (not committed in ProjectSettings).** This is the chosen Decision-C(a) mechanism (the toggle lives in gitignored `EditorUserBuildSettings`); the guard pins the script's `buildAppBundle = true` assignment as the reproducible source. Recorded so a future reader does not mistake the absence of a committed ProjectSettings `.aab` field for an omission. Owner: n/a (by design). [Assets/Editor/VeilwalkersBuilder.cs]
- **[CR-deferred — sanctioned side-effect] The build script mutates `EditorUserBuildSettings.buildAppBundle`/active build target as a deliberate side effect (not restored).** The whole point is to make `.aab` + Android-target reproducible; restoring them after a build would defeat the purpose. Recorded as an accepted side effect (a developer running the build intends the Android/.aab state). Owner: n/a (by design); revisit only if a non-Android build path is ever added. [Assets/Editor/VeilwalkersBuilder.cs]

## Deferred from: code review of 7-1-reserve-the-pit-seam-without-building-it (2026-06-16)

7.1 is the **Pit seam-reservation** story — the FIRST + ONLY Epic-7 story, architecture-only / post-MVP / ZERO gameplay. It ships a `Assets/Veilwalkers/Pit/README.md` (the carried FR-15/16/17 specs + the `ArSessionService`/`EncounterService` reuse note + the one-way Pit→MVP dependency rule + the economy-canon caveat, all cited) + a non-vacuous regression guard `PitSeamReservedTests.cs` (no `.asmdef` / no gameplay `.cs` under the Pit folder; the Pit assembly absent from the graph) — mutation-tested in dev (probe → RED → removed). NO production C#, NO assembly, NO scene/SO/schema/Bootstrap/matrix change. 674/674 EditMode green (+4 over the 670 6.6 baseline). Adversarial 3-layer CR: **13 raw → 13 deduped → 5 confirmed (3 code patches applied, all to `PitSeamReservedTests.cs`: dot-boundary Pit-name match + a robust `ReadAsmdefName` helper guarding empty-path/IO/JSON-parse + the `.gitkeep` message wording), 5 deferred (ALL pre-existing `AcyclicDependencyTests` robustness, code 7.1 did NOT author), 2 spec-sanctioned, 1 false-positive, 0 AC violations.** The 5 deferrals below are an **architectural test-hardening class** in the *pre-existing* `AcyclicDependencyTests` (the sibling the 7.1 guard's enumeration was modeled on) — they affect that file identically and are NOT introduced by 7.1; the same defensive posture was already applied to the NEW 7.1 code via the `ReadAsmdefName` helper (the patches above). They belong together as one consistency pass, not a 7.1 blocker.

- **[CR-deferred] `AcyclicDependencyTests.LoadVeilwalkersAsmdefGraph` does `File.ReadAllText(path)` (line ~205) with no exception handling.** If an `.asmdef` is deleted/becomes unreadable between `FindAssets` and the read (a CI race), the test crashes with an unhandled `FileNotFoundException`/`UnauthorizedAccessException` instead of failing cleanly. Pre-existing; the NEW 7.1 guard already guards this via `ReadAsmdefName` (CR patch). Owner: a future `Architecture.Tests` deserialization/IO-hardening pass (apply the same guard to the sibling). [Assets/Tests/EditMode/Architecture.Tests/AcyclicDependencyTests.cs (LoadVeilwalkersAsmdefGraph)]
- **[CR-deferred] `AcyclicDependencyTests` `JsonUtility.FromJson<AsmdefShape>(json)` (line ~206) has no exception handling.** Malformed asmdef JSON throws `ArgumentException`; the `?.name`/null check only handles a null return, not a parse throw → opaque crash instead of "which asmdef is corrupt." Pre-existing; 7.1's `ReadAsmdefName` already try-catches this in the new code. Owner: the same `Architecture.Tests` hardening pass. [Assets/Tests/EditMode/Architecture.Tests/AcyclicDependencyTests.cs (LoadVeilwalkersAsmdefGraph)]
- **[CR-deferred] `AcyclicDependencyTests.ResolveReferenceName` does `File.ReadAllText(refPath)` (line ~265) on a GUID-resolved reference with no exception handling.** A TOCTOU race between `GUIDToAssetPath` and the read throws unhandled. Pre-existing; not exercised by 7.1's guard (which reads the top-level `name` only, not references). Owner: the same hardening pass. [Assets/Tests/EditMode/Architecture.Tests/AcyclicDependencyTests.cs (ResolveReferenceName)]
- **[CR-deferred — robustness class, both files] The broader "malformed/unreadable asmdef JSON crashes the architecture tests" class** spanning `AcyclicDependencyTests` (graph load + reference resolve) — the 7.1 guard's new code is already hardened (the `ReadAsmdefName` helper); the sibling is not. Best fixed as one consistency task that extracts a shared safe-read helper. Owner: a future `Architecture.Tests` consolidation (a shared guarded `ReadAsmdefName`/`ReadAsmdefShape`). [Assets/Tests/EditMode/Architecture.Tests/AcyclicDependencyTests.cs]
- **[CR-deferred — minor input-validation nit] `AcyclicDependencyTests.ResolveReferenceName` `reference.Substring("GUID:".Length)` (line ~258)** produces an empty GUID for a malformed `"GUID:"` reference, wasting a `GUIDToAssetPath` lookup (already handled safely by the downstream `isEmpty` check on line ~260 — no correctness bug, just a redundant call). Pre-existing. Owner: the same hardening pass (early-detect malformed `"GUID:"`). [Assets/Tests/EditMode/Architecture.Tests/AcyclicDependencyTests.cs (ResolveReferenceName)]

## Deferred from: development + code review of 6-6-meet-the-accessibility-floor (2026-06-16)

6.6 is the **accessibility floor** story — the SIXTH and LAST Epic-6 story. It SETTLES the standing 4.7 "the reduced-motion SETTING + its persistence is Epic 6" + "wiring the setting into `MaterializationView`" defers and the 6.2 "the broader accessibility floor (reduced-motion, TalkBack, contrast, ≥48dp) is 6.6" defer. It delivers the pure-logic accessibility decision layer in `Veilwalkers.UI/Accessibility/` (the `IReducedMotionSetting` port + the concrete `SaveModelReducedMotionSetting` adapter, `AccessibilityRole`/`AccessibilityLabel`/`AccessibilityLabels`, `AccessibilityAnnouncer`, `AccessibilityContrast`) + the reduced-motion taming additions to the 4.7 `MaterializationPresenter`/`Plan`/`Ambiance` (screen-shake + tamed ambiance intensity) + the additive `SaveModel.ReducedMotion` flag + the `PumpkinPatchTokens.MinTapTargetDp = 48` token. Logic-only (the [[ui-presenter-pattern]] altitude split). Adversarial 3-layer CR: **25 raw → 25 deduped → 8 confirmed (all PATCHED — 6 distinct fixes after dedup, incl. 2 AC-violation-flagged: the pack-card-label gap + the missing SetReducedMotion write-path test), 4 deferred, 8 spec-sanctioned, 5 false-positive.** 670/670 EditMode green (+48 over the 622 6.5 baseline). NO production asmdef edge (UI→Persistence/Economy/Monsters/Core/AR all present); NO test asmdef edit; NO acyclic-matrix edit; NO Bootstrap change; NO schema bump / migration (additive bool). The genuine forward-work items are below — the standing Epic-6 view/platform-binding defer + four CR deferrals.

- **The actual platform RENDER/BINDING is the Epic-6 asset-authoring / device build (the standing view-layer defer, D2).** 6.6 delivers the DECISION + TOKENS + descriptors: WHICH motion is tamed (`MaterializationPlan.ScreenShake`/`Ambiance.EffectiveIntensity`/`BrandedGlitch` under `ReducedMotion`), WHICH caption cue is required (`RequiresCaptionCue`), the ≥48dp floor token, the role+state label string (`AccessibilityLabel.ToScreenReaderString`), the announce strings (`AccessibilityAnnouncer`), the contrast firewall (`SafetyTextColor`). The actual reduced-motion taming TWEEN (the static branded transition), the screen-shake amplitude, the audio-sting + caption RENDER, the UGUI ≥48dp min-size enforcement, the dynamic-type reflow, and the platform TalkBack binding (`AndroidJavaObject`/`AccessibilityManager.announce` / the accessibility hierarchy) + the live event subscription (the deferred view subscribes to `OnCreditsChanged`/`OnMonsterDiscovered`/`OnCodexCompleted`, calls the announcer, and announces — with symmetric subscribe/unsubscribe per [[failure-path-cleanup-parity]], the burden of the SUBSCRIBING view, NOT the pure announcer) all remain deferred. Owner: Epic-6 asset-authoring / the device build. [Assets/Veilwalkers/UI/Accessibility/*; Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (Render)]
- **[CR-deferred] `AccessibilityAnnouncer.AnnounceDiscovery(0, 0)` (degenerate empty universe) → "0 of 0 discovered", not a completion variant.** The completion guard is `universe > 0 && discovered >= universe`, which deliberately excludes an empty universe (a 0-monster universe is architecturally nonsensical — the lore fixes 67). A defensive-robustness hardening for invalid count inputs, not a 6.6 AC (which pins the nominal 12/67 + the 67/67 completion). Owner: a future count-announce-input-hardening pass. [Assets/Veilwalkers/UI/Accessibility/AccessibilityAnnouncer.cs (AnnounceDiscovery)]
- **[CR-deferred — test-strength nit] `AccessibilityLabel.ToScreenReaderString` empty-label test validates the contract but is not mutation-strong.** The empty-label → empty-string pin validates the NFR-3-graceful contract correctly, but does not independently exercise each `string.IsNullOrEmpty` filter / the `string.Join` mechanism. A test-infrastructure (mutation-harness) improvement, not a production defect (the code is correct). Owner: a future test-strength / mutation-harness pass. [Assets/Tests/EditMode/UI.Tests/AccessibilityLabelTests.cs]
- **[CR-deferred] `MaterializationPresenter.ShakesByDefault` has no out-of-range `Rarity` test.** Covers the 5 real append-only tiers (AC-1); an out-of-range cast cannot occur until the `Rarity` enum gains a member, which follows the standard `DreadScaleTokens.ForTier` graceful-degrade precedent and owns updating the test (the same family as the 4.7 "no out-of-bounds Rarity test" sanction). Owner: whoever adds a 6th `Rarity` tier. [Assets/Tests/EditMode/UI.Tests/MaterializationPresenterTests.cs (ShakesByDefault)]
- **[CR-deferred — doc nit, no functional impact] The dev-record test-count breakdown summed to 43 while the headline said "+41" for the 663 dev run.** Both are administrative — all tests are present + green (the net is +48 over 622 post-CR). A documentation-correction nit; the functional delivery is unaffected. Owner: this record (noted; no code change). [_bmad-output/implementation-artifacts/6-6-meet-the-accessibility-floor.md]

## Deferred from: development + code review of 6-5-apply-diegetic-voice-and-on-brand-state-treatments (2026-06-16)

6.5 is the **diegetic-voice + on-brand-state-treatment** story — the 5th Epic-6 story. It delivers `VeilVoice` (the single diegetic-copy home — the 5 canonical system-moment lines + the total `For(VeilMoment)` map + the structurally-firewalled plain `Safety` group) + `StateTreatmentKind`/`StateTreatment` (a readonly-struct treatment decision with the no-percentage invariant by construction) + `StateTreatmentPresenter` (the dependency-free switch-on-state map over the EXISTING `ArSessionState`/`PlacementOutcome`/`AnchorRestoreResult`/`CameraPermissionState`/Codex-discovery/`ArSafetyGateState` enums). It re-points the two 6.5-deferred placeholders (`CodexDetailViewModel.NotDiscoveredCopy`, `HomeViewModel.RewardControlLabel`) at `VeilVoice` (single source) + lands the plain safety copy in `ArSafetyView`. Logic-only (the [[ui-presenter-pattern]] altitude split). Adversarial 3-layer CR: **9 raw → 9 deduped → 3 confirmed (all PATCHED — 2 doc-comment fixes + 1 tautological-test removal, NO logic change), 3 deferred (all out-of-scope), 2 spec-sanctioned, 1 false-positive, 0 AC violations.** 622/622 EditMode green (+33 over the 589 6.4 baseline). NO production asmdef edit, NO Bootstrap change, NO scene, NO schema bump, NO acyclic-matrix edit. The only asmdef change is the test-only `Veilwalkers.UI.Tests → Veilwalkers.AR` edge (`.Tests`-excluded from the acyclicity matrix — the 5.4/6.3/6.4 precedent). The genuine forward-work items below are the standing Epic-6 view-layer defer + one upstream AR-validation item — NOT new 6.5 logic debt.

- **The actual RENDER of every treatment is the Epic-6 asset-authoring pass / device build (the standing view-layer defer).** 6.5 delivers the DECISION + COPY + descriptor: WHICH treatment (`StateTreatmentKind`), WHICH copy (`VeilVoice`/`StateTreatment.Copy`), and the no-percentage / safety-exception flags. The actual veil-parting WIPE animation (cold-start ritual), the slow-device "more ritual" frames, the plane-coaching card, the "Pulled back through the Veil" restore beat (reusing the Lure materialization VFX), the camera-denied re-grant screen, the Codex slot-flip + count-tick + new-tier silhouette fade-in tween, and the chunky plain safety overlay/card (the `ArSafetyView` `TODO(Epic 6 asset-authoring)`) — plus the `.unity` scenes that bind these — all remain deferred. Owner: Epic-6 asset-authoring / the device build. [Assets/Veilwalkers/UI/Voice/*; Assets/Veilwalkers/UI/ARHud/ArSafetyView.cs (Render); deferred-work.md (the standing Epic-6 view defers)]
- **[CR-deferred] `StateTreatmentPresenter.ForPlacement` forwards `PlacementResult.Message` un-guarded — an empty/null `NeedsCoaching` message yields a blank coaching card.** The `StateTreatment.Copy` null-coalesce prevents any crash (a null `Message` → empty `Copy`, no NRE), but a `NeedsCoaching` `PlacementResult` constructed with an empty `Message` would surface a coaching treatment with no actionable line. Per Decision D5 (sanctioned), 6.5 PASSES THROUGH the placement's own coaching copy — it does not invent or validate coaching copy. The empty-message GUARD belongs to the AR-layer `PlacementResult.NeedsCoaching(string)` factory (Story 3.4), which currently does not validate non-empty. Owner: Story 3.4 / a future `PlacementResult` input-validation pass. [Assets/Veilwalkers/AR/PlacementResult.cs (NeedsCoaching); Assets/Veilwalkers/UI/Voice/StateTreatmentPresenter.cs (ForPlacement)]
- **[CR-deferred — test-narrative nit] The `VeilVoiceTests` disjointness test asserts the two current `Safety` constants ∉ the diegetic set, which its narrative slightly over-claims as a full "firewall."** This SATISFIES AC-2's "no safety string equals any diegetic string" (and a manual desync like `RewardedAction = ArWarningFast` would be caught by the per-constant canonical-wording pins). The comprehensive firewall (no `Safety` can reach a treatment + every non-safety treatment is non-exception) IS pinned in `StateTreatmentPresenterTests`. A future hardening could broaden the `VeilVoiceTests` disjointness check to both directions + the `For()` outputs. Owner: a future VeilVoice firewall-test-hardening pass. [Assets/Tests/EditMode/UI.Tests/VeilVoiceTests.cs (Safety_copy_is_disjoint_from_every_diegetic_line)]
- **[Decision E — settled, scope boundary] 6.5 does NOT re-label the existing chunky-button CTAs** ("Enter AR Hunt" → "Part the Veil", etc.). AC-1 scopes to system-moment voice (loading / error / reward / reveal / locked-slot), a closed set — NOT a global button rename. The existing chunky-button CTA labels are 6.2/6.3 component decisions with their own ALL-CAPS contract + tests; re-labeling them is a separate content/UX decision (and would risk 6.2/6.3 regressions). `VeilVoice.EnterAr` ("Part the Veil") EXISTS and is available for a future scene/view that wants the diegetic AR-entry framing, but wiring it into the Home CTA is out of 6.5's scope. Owner: a future content/UX decision / the Home-scene asset-authoring. [Assets/Veilwalkers/UI/Voice/VeilVoice.cs (EnterAr); docs/epics.md#Story-6.5]

## Deferred from: development + code review of 6-4-onboard-a-new-player-and-disclose-the-camera (2026-06-16)

6.4 is the **onboarding shell** story — the 4th Epic-6 story. It builds the first-launch onboarding CONTENT the 6.3 `AppStateMachine` gate deferred: pure-logic `OnboardingFlow` (Premise → Disclosure → Grant → Complete step machine) + `OnboardingPresenter`/`OnboardingViewModel` + two minimal descriptors (`PremiseCardStyle`, `CoinBurstStyle`) + two narrow ports (`IDisclosureGate`, `IArPrewarmPort`), all in `Veilwalkers.UI`. It CONSUMES (does not rebuild) the 1.7 `FirstLaunchGrant`, the 3.1 `CameraPermissionFlow`, the 6.1 tokens, the 6.2 kit, and the 6.3 `CompleteOnboarding()` gate. Adversarial 3-layer CR: **36 raw → 36 deduped → 6 confirmed (all PATCHED), 0 deferred, 26 spec-sanctioned, 4 false-positive, 1 AC violation (PATCHED — the premise-count desync, fixed to a single source of truth).** 589/589 EditMode green (+25 over the 564 6.3 baseline). NO production asmdef edit, NO test asmdef edit, NO Bootstrap change, NO scene, NO schema bump, NO acyclic-matrix edit. **0 deferrals from the CR itself.** The genuine forward-work items (the SCENE/VIEW/animation layer + the wiring adapters) are below — they are the standing Epic-6 view-layer defer, not new debt.

- **The thin `OnboardingView : MonoBehaviour`, the `Onboarding.unity` scene, and ALL onboarding motion are Story 6.5 + asset-authoring (D2).** 6.4 delivers the logic — the flow, the presenter, the view-model, the descriptors. The actual UGUI widget binding (premise-card swipe, the disclosure card, the coin-burst particle/coin tween, the ENTER-AR-HUNT button render), the scene, and the "veil-parting" onboarding motion all land in 6.5 (diegetic voice + state treatments) + the device build. Owner: Story 6.5 + asset-authoring. [Assets/Veilwalkers/UI/Onboarding/*; docs/epics.md#Story-6.5]
- **The real disclosure-gate + AR-prewarm ADAPTERS are wired with the deferred View (D4/D6).** 6.4 ships the narrow `IDisclosureGate` (adapts onto 3.1 `CameraPermissionFlow.ConfirmDisclosureAndRequest()`) and `IArPrewarmPort` (adapts onto `ArSessionService.PrewarmAsync()`), each with a graceful `No*` no-op for when the camera flow / AR-rig session is an unregistered Bootstrap seam. The CONCRETE adapters (and the deferred `OnboardingView` constructing the presenter from resolved services — the `MaterializationView` precedent) are wired when the onboarding scene/AR-rig lands. NO Bootstrap change in 6.4 (the View owns construction). Owner: Story 6.5 / the AR-rig scene. [Assets/Veilwalkers/UI/Onboarding/IDisclosureGate.cs; IArPrewarmPort.cs; AR/ArSessionService.cs:105]
- **[Decision A — settled NO] No persisted "onboarding seen" flag in 6.4.** 6.4 drives the onboarding FLOW + the `CompleteOnboarding()` gate; whether the onboarding SCREENS re-show on a returning install (a `SaveModel` bool distinct from the 1.7 `StartingCreditsGranted` marker) was deferred — the AC scopes to "a first launch", the credit-grant marker already gates once-per-install, and adding a second persisted flag is scope the AC does not require. If a future UX decision wants explicit re-show control, it is an additive `SaveModel` bool (no schema bump — the `StartingCreditsGranted` additive precedent). Owner: a future onboarding-persistence / UX decision. [docs/epics.md#Story-6.4 (AC-1 "first launch"); Assets/Veilwalkers/Persistence/SaveModel.cs]
- **The denied-permission re-grant path during onboarding is the existing 3.1 flow (inherited, not rebuilt).** If the player denies the camera permission at the disclosure step, the Story-3.1 `CameraPermissionState.Denied` → Settings re-grant flow applies unchanged; 6.4 does not re-implement it and does not add an onboarding-specific denial screen. The onboarding-flow-meets-denial integration (does onboarding pause / how the disclosure card reflects a denial) is a 6.5 view-wiring concern. Owner: Story 6.5. [Assets/Veilwalkers/AR/CameraPermissionFlow.cs; docs/epics.md#Story-6.5]

## Deferred from: development + code review of 6-2-build-the-chunky-shared-component-kit (2026-06-16)

6.2 builds the **chunky shared component kit** (UX-DR3..DR9, UX-DR17) as pure-logic style descriptors (`ChunkyStyle` primitive + `ChunkyButtonStyle`/`CreditPillStyle`/`PackCardStyle`/`CodexSlotStyle`/`RarityBadgeStyle`/`WordmarkStyle`) sourcing ONLY Story-6.1 tokens + ONE representative thin view stub (`ChunkyComponentView`) — the UI-presenter altitude split (the `MaterializationPlan`/`MaterializationView` precedent). 518/518 EditMode green (+31 over the 487 6.1 baseline). NO production asmdef edit (UI→Monsters/Billing/Core already present); the only asmdef change is the test-only `UI.Tests → Billing` edge (excluded from the acyclicity matrix by the `.Tests` suffix — the 5.4 precedent). NO Bootstrap edit, NO schema bump.

- **[SETTLED] The 6.1-CR `PumpkinPatchTokens.Spacing` element-mutability deferral (owner "Story 6.2+").** `Spacing` changed from `public static readonly int[]` to `public static readonly IReadOnlyList<int>` (`System.Array.AsReadOnly` over a private `_spacing` backing array) — the shared design-token scale can no longer be mutated through the public surface (`Spacing[0] = 99` no longer compiles). The `SpaceNN` named consts are unchanged; the only `.Length` call site repo-wide (the 6.1 `PumpkinPatchTokensTests` ascending loop) moved to `.Count`. CLOSES the 6.1-CR deferral below. [Assets/Veilwalkers/UI/Design/PumpkinPatchTokens.cs (Spacing); Assets/Tests/EditMode/UI.Tests/PumpkinPatchTokensTests.cs]
- **The real UGUI render — prefabs, sprites, 9-slice outline art, fonts, `.unity` scenes — is Story 6.3 / asset-authoring.** 6.2 delivers the chunky-style DESCRIPTORS (the single source of fill/outline/shadow/radius/label truth, all from 6.1 tokens) + one thin `ChunkyComponentView` stub whose `Render(ChunkyStyle)` logs (the `MaterializationView.Render` precedent, `TODO(Story 6.3)`). The actual widget binding (UGUI Image/Text, the 9-slice outline, the offset drop-shadow, the ALL-CAPS label / ⬡ glyph / skull icon, the pressed-state transition, the felt-descent pulse tween, the silhouette→art Codex flip, the wordmark wobble motion) all land in 6.3. Owner: Story 6.3 + asset-authoring. [Assets/Veilwalkers/UI/Components/*.cs; docs/epics.md#Story-6.3]
- **Wiring any component to a live service / scene / navigation is Story 6.3 (`AppStateMachine`).** The credit-pill `TapToShop` intent, the live balance binding, the Codex-grid binding, the pack-card Play-price binding, the `OnInsufficientCredits`→top-up navigation are all 6.3 — 6.2 only DECLARES the affordances on the descriptors. Owner: Story 6.3. [docs/epics.md#Story-6.3; docs/architecture.md:524]
- **Onboarding premise/disclosure/coin-burst cards (6.4), diegetic voice/state-treatments (6.5), the accessibility-floor wiring (6.6).** These epics CONSUME the kit but are not built here. 6.2 honors UX-DR17 "SLAY-red never the sole signifier" at the SLAY-button DESCRIPTOR level (the `RequiresIcon` flag + the fixed "SLAY" label — a component-shape decision); the broader accessibility floor (reduced-motion, TalkBack, contrast, ≥48dp tap targets) is 6.6. Owner: Stories 6.4 / 6.5 / 6.6. [docs/epics.md#Story-6.4, #Story-6.5, #Story-6.6]
- **`ChunkyButtonStyle.DisplayLabel` per-read `ToUpperInvariant()` allocation** (CR deferral — Task 2 sanctioned compute-on-read; the per-frame caching belongs with 6.3's widget binding). Owner: Story 6.3. [Assets/Veilwalkers/UI/Components/ChunkyButtonStyle.cs (DisplayLabel)]
- **`PackCardStyle.BadgeTextFor` `default`-folds a future `PackBadge` to empty** (CR deferral — correct for the 3-pack canon; a new badge lands with its arm, the append-only-switch precedent). Owner: a future pack-catalog-expansion story. [Assets/Veilwalkers/UI/Components/PackCardStyle.cs (BadgeTextFor)]

## Deferred from: code review of 6-1-implement-the-pumpkin-patch-design-tokens (2026-06-16)

Adversarial 3-layer CR (Blind Hunter / Edge Case Hunter / Acceptance Auditor) + per-finding verifier: **10 raw → 10 deduped (0 cross-layer convergence) → 1 confirmed (PATCHED), 1 deferred, 6 spec-sanctioned, 2 false-positive. 0 AC violations.** 487/487 EditMode green re-verified after patching (+24 over the 463 5.4 baseline; the patch was a test-comment/assertion correctness fix — no net test-count change). The 1 confirmed patch (recorded in the story's `### Review Findings`): the `ForTier` totality-test comment misstated its guard (a missing-arm collision would KEEP the distinct count, so the `==5` assert wouldn't fail) — fixed to assert `distinct == Enum member-count` (catches duplicate-mapping for real) + pointed the missing-arm guard at the sibling `LogAssert.Expect` warning test. The 1 genuine future-work deferral:

- **`PumpkinPatchTokens.Spacing` is a `public static readonly int[]` — the reference is frozen but the ELEMENTS are mutable** (`Spacing[0] = 99` would silently corrupt the shared design-token scale process-wide). The XML doc calls it "an immutable ordered set," which the array does not enforce at the element level. NOT an AC-2 violation (AC-2 asks for "named constants," satisfied by the `SpaceNN` consts + the array). The fix is to expose `IReadOnlyList<int>` / `ReadOnlyCollection<int>` or return a defensive copy — a token-hardening / consumer-discipline change that belongs with the first consumer that iterates the scale. Owner: Story 6.2+ (the chunky component kit) / a future design-token-hardening pass. [Assets/Veilwalkers/UI/Design/PumpkinPatchTokens.cs (Spacing)]

## Deferred from: code review of 5-4-top-up-mid-encounter-without-losing-the-encounter (2026-06-16)

Adversarial 3-layer CR (Blind Hunter / Edge Case Hunter / Acceptance Auditor) + per-finding verifier: **18 raw → 18 deduped (0 cross-layer convergence) → 8 confirmed (all PATCHED), 2 deferred, 6 spec-sanctioned, 2 false-positive. 0 AC violations.** 463/463 EditMode green re-verified after patching (+4 CR-patch pins). The 8 confirmed patches reduced to 3 root causes (recorded in the story's `### Review Findings`): (1) snapshot/rehydrate now read+mutate the shared encounter state UNDER the mutation lock (the `CommitLureSpendAsync` discipline — `RehydrateFromSnapshot` became `async` for this); (2) the corrupt-snapshot gate now adds the SEMANTIC guards Decision H promised — null/empty `MonsterId` + an anchors-vs-monsters length mismatch; (3) the consume-clear is now AWAITED in-lock with a whole-rehydrate rollback on a clear fault (no more fire-and-forget → no relaunch zombie encounter). The 2 genuine future-work deferrals:

- **The `_activeMonsterIds`↔`_activeAnchors` index-parallel invariant is documented but not architecturally enforced for FUTURE Lure variants.** 5.4 keeps the two arrays index-parallel (filled in placement order in `TryLureAsync`, length-checked on rehydrate) + documents the invariant in the field comment. A future re-Lure / guided-Lure variant that modifies `_activeMonsterIds` without keeping `_activeAnchors` in sync would silently break it — but re-Lure / queued-spawn is explicitly Epic 6 / 6.3, and the story that introduces it owns either following the same placement-order pairing or documenting its own enforcement. Owner: the future re-Lure / queued-spawn story. [Assets/Veilwalkers/Encounter/EncounterService.cs (_activeMonsterIds)]
- **`FakeEncounterProgressStore.Clone` does not validate `AnchorToken` entries for corruption (a `default(AnchorToken)` round-trips faithfully).** Correct per the anti-tautology pattern (the test proves the snapshot round-trips, even invalid data); production is safe because an invalid anchor fails restoration via `AnchorRestoreService.TryRestore` (which checks `AnchorToken.IsValid`) in Story 6.3's scene re-anchor. Per-element anchor-validity detection is outside 5.4's structure-level corruption boundary (Decision H). Owner: a future snapshot-element-validation / defensive-serialization-hardening pass. [Assets/Tests/EditMode/Encounter.Tests/EncounterTestFakes.cs]

## Deferred from: development of 5-4-top-up-mid-encounter-without-losing-the-encounter (2026-06-15)

5.4 is the **encounter-snapshot convergence story** — it builds the headless snapshot/rehydrate ENGINE (`EncounterService.SnapshotActiveEncounter`/`RehydrateFromSnapshot`/`ClearEncounterSnapshot` round-tripping through `SaveModel.EncounterSnapshot` via the now-populated `EncounterSnapshotData`/`EncounterMonsterStateData` DTOs) and SETTLES four inherited encounter-state-persistence deferrals. 460/460 EditMode green (+14 over the 446 5.3 baseline).

- **[SETTLED] The 4.3 "persisted scan-progress snapshot/rehydrate disk round-trip is Story 5.4" deferral.** `EncounterService._scannedThisEncounter` now round-trips via `EncounterMonsterStateData.ScanProgress` — Story 4.3's boolean-grained "scanned this encounter" maps to the DTO's `float [0,1]` as 0.0/1.0 (a Scan sets 1.0; on rehydrate `ScanProgress > 0` re-adds the id to the per-encounter scanned set). A FRACTIONAL multi-tick scan mechanic that would populate the float with intermediate values is still a future gameplay/balancing story (the 4.3 float-meaning note stands). [Assets/Veilwalkers/Encounter/EncounterService.cs (BuildSnapshotDto / RehydrateFromSnapshot); Assets/Veilwalkers/Persistence/EncounterMonsterStateData.cs (ScanProgress)]
- **[SETTLED] The 4.6 "persisted disk snapshot of the active extras across a Shop navigation / process kill is Story 5.4" deferral.** `EncounterService._activeExtras` now round-trips via a NEW encounter-level `EncounterSnapshotData.AppliedExtras` (stable string tags `"StabilityBoost"`/`"NightveilFilter"`, NOT the enum int — the telemetry-stability precedent; mapped via an explicit switch with an `Enum.TryParse` forward-compat fallback). Stored at the ENCOUNTER level (not per-Monster `EncounterMonsterStateData.AppliedBoosts`, which stays reserved for a future per-monster boost mechanic) because `_activeExtras` is an encounter-wide `HashSet<ExtraKind>` ("for the remainder of the current encounter"). [Assets/Veilwalkers/Persistence/EncounterSnapshotData.cs (AppliedExtras); Assets/Veilwalkers/Encounter/EncounterService.cs (BuildSnapshotDto / RehydrateFromSnapshot / MapExtraKind / TryMapExtraTag)]
- **[SETTLED] The 5.1/5.2 "the mid-encounter top-up sheet over a live encounter + the Encounter-survives-Shop-round-trip snapshot/rehydrate is Story 5.4" deferral (the ENGINE half).** `SnapshotActiveEncounter` (capture the live `Lured` encounter — anchors + lured ids + per-id scan flag + applied extras + the state tag — in ONE atomic write, AR-8), `RehydrateFromSnapshot` (restore the LOGICAL encounter from the persisted DTO, consume-once), and `ClearEncounterSnapshot` (the normal-end clear) all LAND. The new `_activeMonsterIds` field (index-parallel with `_activeAnchors`) retains the lured ids the snapshot needs (previously built in `TryLureAsync` and discarded). AC-1 "the world does not lock" was already true (an insufficient-credit SLAY resolves back to `Lured` + raises `OnInsufficientCredits` — 4.5) and is now pinned as a regression guard; AC-3 "AR Safety Warning does NOT re-fire" was already 3.2's `ArEntryKind.ShopResume`; AC-4's exactly-once purchase reconcile was already 5.2's `ReconcilePendingOnLaunchAsync` — 5.4 proves the encounter-rehydrate and the purchase-reconcile compose on relaunch (the headline AC-4 pin). [Assets/Veilwalkers/Encounter/EncounterService.cs (SnapshotActiveEncounter / RehydrateFromSnapshot / ClearEncounterSnapshot / _activeMonsterIds); Assets/Veilwalkers/Persistence/EncounterSnapshotData.cs (State); Assets/Veilwalkers/Persistence/SaveModel.cs (EncounterSnapshot — populated, previously reserved)]
- **DEFER to Epic 6 / Story 6.3 (the `AppStateMachine` + the UI-presenter altitude split):** (a) the non-blocking top-up SHEET pixels (the one-level-deep stacking over the live encounter, the calm/non-alarming message on cancel — UX-DR13); (b) the `AppStateMachine` navigation that DECIDES Shop/top-up on `OnInsufficientCredits` (architecture.md:524, epics.md:889-891 — "AppStateMachine (not a service) decides Shop/top-up navigation") and CALLS `SnapshotActiveEncounter` before Shop + `RehydrateFromSnapshot` on return + `RequestEntry(ArEntryKind.ShopResume)` (not `ColdEntry`) on resume + `ClearEncounterSnapshot` when an encounter ends after a Shop round-trip; (c) the SCENE re-anchor / pooled re-spawn / materialization-replay RENDER on rehydrate — 5.4 restores the LOGICAL encounter (anchors + ids + scan + extras + state-machine state); `_activeSpawnHandles` is process-local (released on teardown), NOT snapshotted, so the visual re-spawn is 6.3 (the `Recover` "scene re-anchor is Story 6.3" precedent, EncounterService.cs; the 4.7 render-defer altitude split). Owner: Epic 6 / Story 6.3. [docs/epics.md#Story-6.3; docs/architecture.md:524-525]
- **NO new production assembly edge, NO schema bump.** The DTOs live in `Veilwalkers.Persistence`, which `Veilwalkers.Encounter` already references — every type 5.4 touches is reachable (NO `AcyclicDependencyTests` matrix edit, contrast 5.2's Billing→Persistence edge). The DTO field additions (`State`, `AppliedExtras`) are additive — no shipped save ever wrote an `EncounterSnapshot` (it was reserved-but-unused), so there is no old data to migrate (`SaveMigrations.CurrentVersion` stays 2; no `MigrateV2ToV3`). A TEST-ONLY asmdef edge was added (`Veilwalkers.Encounter.Tests` → `Veilwalkers.Billing`) so the AC-4 relaunch pin can drive the real `PurchaseReconciler` + the real rehydrate over ONE shared stored model (`.Tests` assemblies are excluded from the acyclicity matrix). No store/SDK/IAP surface touched (the purchase half is the existing 5.1/5.2 path). [Assets/Tests/EditMode/Encounter.Tests/Veilwalkers.Encounter.Tests.asmdef (test-only Billing ref)]

## Deferred from: development of 5-3-grant-and-consume-the-guaranteed-rare-lure (2026-06-15)

- **[SETTLED] The 5.1 + 5.2 "the Guaranteed-Rare Lure GRANT/CONSUME is Story 5.3" deferral.** Both halves LANDED: (a) the GRANT — `PurchaseReconciler.AdvanceToGrantedAsync` now increments `SaveModel.GuaranteedRareLures` by one for a Veil pack (gated on `CreditPack.IncludesGuaranteedRareLure`), folded into the SAME `Granted`-state persist as the Credits so the 5.2 order-id + persisted-state dedup makes the lure grant exactly-once across interruption for free (a `Granted` record grants neither Credits nor a second lure); (b) the CONSUME — a new `LureKind.GuaranteedRare` routes through the EXISTING `EncounterService.TryLureAsync` → `LureSystem.TryRollGuaranteedRare` (forces `Rarity >= RarityThresholds.GuaranteedRareFloor`, NO common fallback), consuming one one-shot item (cost 0, NOT Credits) via the new `CommitGuaranteedRareLureConsumeAsync` composed write (exactly-once, guarded-decrement, AR-8), with `RefundGuaranteedRareLureAsync` on a post-consume spawn refusal and a distinct `LureFailureReason.NoGuaranteedRareLure` for a zero-inventory attempt (NOT a credit shortfall — `OnInsufficientCredits` is NOT raised). [Assets/Veilwalkers/Billing/PurchaseReconciler.cs (AdvanceToGrantedAsync); Assets/Veilwalkers/Encounter/LureSystem.cs (TryRollGuaranteedRare); Assets/Veilwalkers/Encounter/EncounterService.cs (TryLureAsync GuaranteedRare branch + the consume/refund siblings); Assets/Veilwalkers/Persistence/SaveModel.cs (GuaranteedRareLures)]
- **The Shop "use your Guaranteed-Rare Lure" affordance + the spend-ack pixels are Epic 6** (the UI-presenter altitude split). 5.3 surfaces the inventory count (`SaveModel.GuaranteedRareLures`), the typed `NoGuaranteedRareLure` reason, and the `LureKind.GuaranteedRare` entry point; the chunky "you have N Guaranteed-Rare Lures — use one" button + the on-brand confirmation are Epic 6's. Owner: Epic 6. [docs/epics.md#Epic-6]
- **NO schema-version bump / migration was needed for the new `int GuaranteedRareLures` field** (Decision A — the additive-int precedent): an existing v2 save that lacks the JSON property deserializes it to 0 ("owns none"), exactly how `StrongCaptureCharges`/`StabilityBoostCharges`/`NightveilFilterCharges` were added. `SaveMigrations.CurrentVersion` stays 2; no `MigrateV2ToV3`. (Recorded here so a future reader does not mistake the absence of a migration for an omission.) [Assets/Veilwalkers/Persistence/SaveModel.cs; SaveMigrations.cs]
- **No device-build / IAP-import / `IPurchaseVerifier` work was touched.** 5.3 adds NO store/SDK surface (the grant rides the existing 5.2 reconciler; the consume is pure Encounter/Economy logic over `LureSystem` + the local save). The device-only `UnityIapStoreAdapter` acknowledge/purchase deferrals + the Phase-2 server verifier (and the narrow grant↔state-advance two-write window the lure now shares) all stand exactly as recorded for 5.2 — unchanged. [deferred-work.md → "development of 5-2" entries]

## Deferred from: code review of 5-2-reconcile-purchases-exactly-once (2026-06-15)

Adversarial 3-layer CR (Blind Hunter / Edge Case Hunter / Acceptance Auditor) + per-finding verifier: **23 raw → 23 deduped (0 cross-layer convergence) → 8 confirmed (all PATCHED), 2 deferred, 10 spec-sanctioned, 3 false-positive. 0 AC violations.** 433/433 EditMode green re-verified after patching. The 8 confirmed patches are recorded in the story's `### Review Findings` (the in-line-purchase result contract self-consistency, the sync-over-async lock helpers → async, and the corrupt-record guards: non-positive total / unrecognized state / empty order id). The 2 genuine future-work deferrals:

- **Concurrent in-process reconcile TOCTOU (two overlapping passes on the same order id could both grant).** `ReconcileOneAsync` reads the record state under a short lock, RELEASES it, then grants outside the lock (the lock must not be held across `GrantCreditsAsync`, which acquires the same non-reentrant lock — a deadlock). The persisted-`Granted`-state advance defends the SEQUENTIAL app-kill recovery path AC-2 targets ("granted exactly once on the NEXT launch"), but two concurrent in-process passes (e.g. the fire-and-forget launch pass overlapping a fresh in-line `ReconcilePurchaseAsync`) could both read `PendingGrant` and both grant. AC-2 scopes exactly-once to next-launch sequential recovery, not concurrent in-process overlap; closing the concurrent window needs an in-memory in-flight-order guard OR serializing the launch pass against the in-line path. Owner: a future Billing concurrency-hardening story / the Phase-2 verifier. [Assets/Veilwalkers/Billing/PurchaseReconciler.cs (ReconcileOneAsync — the read-state→grant→advance span)]
- **`ClearRecordAsync` (and the grant/ack-fault branches) loop indefinitely on UNRECOVERABLE save-file corruption.** Each persist-fault branch logs a Warn + leaves the record for the next pass (the spec's sanctioned retry-on-fault pattern — matching the grant-fault + ack-fault branches). A TRANSIENT I/O fault self-heals on a later pass; an UNRECOVERABLE corruption (e.g. a `JsonException` on every load) re-fails forever. This is a save-LAYER failure, not a billing-logic defect, and the MVP is client-trusted (no server verifier to break the loop). A retry-count-then-abort field on `PendingPurchaseRecord` (or a Persistence-layer corruption-recovery flow) is the fix. Owner: Phase-2 server verification / a save-recovery hardening pass. [Assets/Veilwalkers/Billing/PurchaseReconciler.cs (ClearRecordAsync catch); docs/architecture.md:497-498]

## Deferred from: development of 5-2-reconcile-purchases-exactly-once (2026-06-15)

- **The REAL Play acknowledge/consume SDK call (`UnityIapStoreAdapter.AcknowledgeAsync`'s `#if UNITY_ANDROID && !UNITY_EDITOR` body) is the device-build / IAP-import task.** Same owner + posture as the 5.1 `UnityIapStoreAdapter.PurchaseAsync` SDK-glue deferral: `com.unity.purchasing` (5.0.0+, Play Billing 8) is not imported, so the device path is a `TODO(Story 6.3 / device build)` that will call the `IStoreController` `ConfirmPendingPurchase` / Play `acknowledgePurchase|consumeAsync` keyed by the order id. The editor `#else` returns a no-op success (true) so the reconcile pass clears the ledger in-editor; the reconciler's acknowledge SEQUENCE + retry-on-false logic is proven entirely headless against `FakeStoreAdapter` (the `AcknowledgeAsync` call-counter + acknowledged-order-ids). Owner: the device-build / IAP-import session. [Assets/Veilwalkers/Billing/UnityIapStoreAdapter.cs (AcknowledgeAsync)]
- **`AlreadyOwned` is still SURFACED-only, not granted, in 5.2 (the reconciler does not consume it).** The 5.1 `AlreadyOwned` typed result is unchanged: routing an already-owned purchase through the reconciler requires the device-only Play "query owned/un-consumed purchases" call to obtain the order id the reconciler dedups on, and the editor stub cannot synthesize one. So 5.2 grants `AlreadyOwned` NOTHING (the 5.1 test stands) — the order-id dedup machinery is built and ready, but the device-side "recover an owned purchase" wiring lands with the real Play acknowledge SDK glue (the architecture AR-12 "reconcile pending on every launch" intent is satisfied for INTERRUPTED purchases via the pending-ledger; the OWNED-but-never-recorded case needs the device query). Owner: the device-build / IAP-import session (alongside the real `AcknowledgeAsync`). [Assets/Veilwalkers/Billing/BillingService.cs (MapStoreOutcome AlreadyOwned); Assets/Veilwalkers/Billing/PurchaseReconciler.cs]
- **The narrow grant-persist↔state-advance two-write window is a documented client-trusted MVP exposure.** The reconciler grants via `GrantCreditsAsync` (its own atomic persist) and THEN persists the record's `Granted`-state advance as a SECOND write. A process kill EXACTLY between those two persists leaves Credits granted with the record still `PendingGrant` on disk, so the next launch pass would re-grant (a double-grant) — UNLESS a server-side verifier catches it. This is the residual edge of being client-trusted (architecture.md:497-498): the order-id record exists for a verifier to reconcile, and the window is a single inter-persist instant (vs the wide app-kill-before-crediting window that the pending-ledger fully closes). The robust fix is the Phase-2 `IPurchaseVerifier` (server-authoritative grant ledger) OR folding the grant + state-advance into ONE persist (which would require the reconciler to own the credit mutation rather than reuse `GrantCreditsAsync` — rejected, as it duplicates the battle-tested grant pipeline). Logged loudly (`GameLog.Error`) when it occurs. Owner: Phase-2 server verification. [Assets/Veilwalkers/Billing/PurchaseReconciler.cs (ReconcileOneAsync, the AdvanceToGrantedAsync failure branch); docs/architecture.md:497-498]
- **The "non-alarming message" + "return to prior context" Shop UI (AC-3 pixels) is Epic 6 / Story 5.4.** 5.2 surfaces the typed `PurchaseResult`/reason (cancel/fail still return `Cancelled`/`StoreUnavailable`, balance unchanged, no pending record); the HUD treatment that shows a calm message + the mid-encounter top-up-sheet return-state-faithful binding is the UI-presenter altitude split. Owner: Epic 6 / Story 5.4. [docs/epics.md#Story-5.4]
- **`IPurchaseVerifier`/`LocalPurchaseVerifier` (client-trusted MVP) stays reserved.** 5.2 operates client-trusted (architecture.md:497-498); server-side purchase verification is the Phase-2 trigger (the 5.1 deferral stands — and it is the robust fix for the two-write window above). Owner: a Phase-2 server-verification story. [docs/architecture.md:419, :497-498]
- **[SETTLED] The 5.1 "exactly-once reconciliation is Story 5.2" deferral.** The `PurchaseReconciler` + the pending-ledger (`SaveModel.PendingPurchases` / `PendingPurchaseRecord`, the State vocabulary now defined: `pending-grant` → `granted` → cleared) + order-id dedup + acknowledge-within-window (AC-4) + interruption survival (AC-2, the launch recovery pass) all LANDED. `BillingService`'s single grant site now routes through `PurchaseReconciler.ReconcilePurchaseAsync` with NO public-surface change (the 5.1 `BillingServiceTests` stayed green). The 5.1 `GrantFailed` reason flipped from "lost until 5.2" to "recovered exactly-once on the next launch." [Assets/Veilwalkers/Billing/PurchaseReconciler.cs; BillingService.cs]
- **[SETTLED] The 5.1-CR-deferred "`MapStoreOutcome` default-arm exhaustiveness is a 5.2 decision" item.** 5.2 added NO new `StorePurchaseOutcome` value (the reconciler works on the EXISTING `Purchased` store result + the pending-ledger, not a new store outcome), so the 5.1 `default`-folds-to-`StoreUnavailable` arm stands unchanged — there is no new outcome for it to silently mis-map. The throw-on-unknown-vs-fold call remains a future-hardening choice if a later story adds an outcome. Owner: a future Billing factory-hardening pass (unchanged from the 5.1 defer). [Assets/Veilwalkers/Billing/BillingService.cs (MapStoreOutcome)]

## Deferred from: code review of 5-1-browse-and-purchase-credit-packs-via-google-play-billing (2026-06-15)

Adversarial 3-layer CR (Blind Hunter / Edge Case Hunter / Acceptance Auditor) + per-finding verifier: **17 raw → 17 deduped (0 cross-layer convergence) → 0 confirmed, 0 patched, 4 deferred, 8 spec-sanctioned, 5 false-positive. 0 AC violations.** No code changed — the 420/420 EditMode suite from dev stands. Every finding was real-signal-but-out-of-5.1-scope or a misread of a structurally-guaranteed invariant (the immutable `CreditPackCatalog` + the `CreditPack.Create` positive-base guard make several "defensive check" findings unreachable). The 8 spec-sanctioned + 5 false-positive are recorded in the story's `### Review Findings`; the 4 genuine future-work deferrals are below.

- **`MapStoreOutcome` lacks a guard for the structurally-impossible `StorePurchaseOutcome.None` case.** `MapStoreOutcome` (BillingService.cs) runs only when `store.Purchased == false`, which means the result came from `StorePurchaseResult.NotCompleted`, whose factory throws on `None` (StorePurchaseResult.cs:79-83) — so `None` can never reach the switch, which silently folds it into `StoreUnavailable`. A defensive assert for the impossible case (so a future adapter/refactor producing `Purchased=false`+`Outcome=None` fails loudly rather than mis-reporting) is a factory-hardening concern, not a 5.1 defect (the `ScanResult.Succeeded` validate-only-at-the-factory precedent — re-validating in the private mapper duplicates the guard for no benefit). Owner: a future Billing factory-hardening / defensive-assertion pass. [Assets/Veilwalkers/Billing/BillingService.cs (MapStoreOutcome); Assets/Veilwalkers/Billing/StorePurchaseResult.cs:79-83]
- **No `int.MaxValue` overflow boundary test / `CreditPack.Create` sum-safety validation for a very large pack grant.** The three canon packs (50/170/500) pose no overflow risk, and `CreditService.GrantCreditsAsync`'s `checked` block (the documented programmer-error guard, CreditService.cs:167-173) already converts an overflowing grant to a throw — but neither the Billing tests pin a near-`int.MaxValue` pack nor does `CreditPack.Create` validate `BaseCredits + BonusCredits ≤ int.MaxValue`. A future pack mistakenly defined with a huge bonus would overflow at grant time. Belongs to a later pack-definition story (5.3+) / OQ-9 balancing, or the recurring Economy overflow-hardening pass (the same family as the 4.2 `RefundLureSpendAsync` + 1.8 daily-claim overflow defers — a centralized `checked` credit mutator + boundary tests across every Economy mutator/caller). Owner: Story 5.3+ / a future Economy overflow-hardening pass. [Assets/Tests/EditMode/Billing.Tests/BillingServiceTests.cs; Assets/Veilwalkers/Billing/CreditPack.cs (Create); Assets/Veilwalkers/Economy/CreditService.cs:167-173]
- **`MapStoreOutcome`'s `default` arm silently maps a future `StorePurchaseOutcome` value to `StoreUnavailable` (no exhaustiveness enforcement).** When Story 5.2 adds a new outcome (e.g. a reconciler `PendingConsumption`), it lands WITH its `MapStoreOutcome` arm in the same commit (no production timing gap), so this is correct for 5.1's defined four outcomes. Whether to make the `default` an explicit throw (forcing future exhaustiveness) is a 5.2 implementation decision. Owner: Story 5.2. [Assets/Veilwalkers/Billing/BillingService.cs (MapStoreOutcome default)]
- **`CreditPackCatalog` lookup is case-sensitive (`StringComparer.Ordinal`); a case-mismatched `packId` resolves as `UnknownPack`.** The invariant is "Play returns canonical lowercase product ids"; a mismatch correctly returns the already-tested `UnknownPack` path (no charge — the lookup precedes any store call). Defensive case-normalization (ordinal-ignore-case lookup) is a 5.2+ Billing-hardening concern, not a 5.1 AC. Owner: a future Billing-hardening story. [Assets/Veilwalkers/Billing/CreditPackCatalog.cs (_byId StringComparer.Ordinal, TryGet)]

## Deferred from: development of 5-1-browse-and-purchase-credit-packs-via-google-play-billing (2026-06-15)

- **Exactly-once reconciliation (`PurchaseReconciler` + the pending-ledger + order-id-keyed dedup + acknowledge-within-window) is Story 5.2.** 5.1 grants once per successful `PurchaseAsync` via `ICreditService.GrantCreditsAsync` directly (the simple happy path) + surfaces a distinct `PurchaseFailureReason.GrantFailed` (store charged but the local grant did not durably save) + a `PurchaseFailureReason.AlreadyOwned` reason + carries the Play order id on `StorePurchaseResult` — all the 5.2 seams. The canonical flow (architecture.md:522) routes `BillingService → Unity IAP → PurchaseReconciler (pending→ack→credit once→persist) → CreditService`; 5.1 grants in ONE place (the `Purchased` branch of `BillingService.PurchaseAsync`) so 5.2 can interpose the reconciler without rewriting the public surface. 5.1 does NOT survive interruption/app-kill or dedup by order id. The `SaveModel.PendingPurchases` ledger field already exists (reserved). Owner: Story 5.2 (NFR-4). [Assets/Veilwalkers/Billing/BillingService.cs (PurchaseAsync, the grant site); Assets/Veilwalkers/Persistence/SaveModel.cs (PendingPurchases); docs/architecture.md:419-420, :497, :510, :522]
- **The Guaranteed-Rare Lure GRANT/CONSUME is Story 5.3.** 5.1's Veil pack DECLARES `CreditPack.IncludesGuaranteedRareLure = true` (catalog transparency, AC-1) but grants NOTHING beyond the Credits; granting the one-shot item (via `PurchaseReconciler`) + consuming it through `LureSystem` (the reserved `LureKind.GuaranteedRare` + `RarityThresholds.GuaranteedRareFloor`) is Story 5.3. 5.1 touches neither `LureKind` nor `LureSystem`. Owner: Story 5.3. [Assets/Veilwalkers/Billing/CreditPack.cs (IncludesGuaranteedRareLure); docs/epics.md#Story-5.3]
- **The Shop UI (the chunky catalog surface, the localized-price binding, the buy affordances, the `OnPurchaseCompleted` HUD binding, the non-blocking top-up sheet) is Epic 6 / Story 5.4.** 5.1 registers `IBillingService` LIVE + returns the typed `PurchaseResult` + raises `OnPurchaseCompleted` + exposes `Packs`/`FetchLocalizedPricesAsync` for the UI to read; the pixels are Epic 6 (the UI-presenter altitude split — the CameraPermissionView/ArSafetyView precedent: the consumer resolves `IBillingService` via `GameServices.Get` + degrades while unplaced). The mid-encounter top-up sheet over a live encounter (binding `OnInsufficientCredits` → Shop → return state-faithful) is Story 5.4. Owner: Epic 6 / Story 5.4. [Assets/Veilwalkers/Billing/IBillingService.cs; docs/epics.md#Story-5.4; UX Shop surface]
- **The Unity IAP package import + the device-only `UnityIapStoreAdapter` SDK glue is a device-build / IAP-import task.** Like ARCore Extensions (the Story 1.1 import deferral + the EDM4U Gradle deferral), `com.unity.purchasing` 5.0.0+ (bundles Play Billing 8.0.0) is a pinned package added at IAP-import time; the `#if UNITY_ANDROID && !UNITY_EDITOR` real `IStoreController`/`IStoreListener` purchase-flow wiring (initialize products from the catalog ids, `InitiatePurchase`, map `PurchaseEventArgs`→`StorePurchaseResult`, read `localizedPriceString`) is a `TODO(Story 6.3 / device build)`. The editor `#else` path compiles + is EditMode-reachable regardless (reports a non-completed `Failed` store result + no localized prices — the `ArcoreAnchorProvider` stub-safe-editor-path posture), so the `BillingService` decisions are fully proven headless against `FakeStoreAdapter`. The package being absent is also why `UnityIapStoreAdapter` references NO Unity IAP type yet. Owner: the device-build / IAP-import session. [Assets/Veilwalkers/Billing/UnityIapStoreAdapter.cs; docs/architecture.md:137, :772-774]
- **`IPurchaseVerifier`/`LocalPurchaseVerifier` (architecture.md:419) — the client-trusted MVP verification posture — is reserved, not built.** Architecture documents purchases are client-trusted in MVP (no server verification; architecture.md:497-498) with the Phase-2 trigger being server-side verification. The `IPurchaseVerifier` seam is named for that future hook; 5.1 (and 5.2) operate client-trusted. Owner: a Phase-2 server-verification story (post-MVP). [docs/architecture.md:419, :497-498]

## Deferred from: code review of 4-7-materialize-monsters-with-tiered-accessible-entrances (2026-06-15)

Adversarial 3-layer CR (Blind Hunter / Edge Case Hunter / Acceptance Auditor) + per-finding verifier: 8 raw → 8 deduped (0 cross-layer convergence) → **2 confirmed (both PATCHED), 1 deferred, 4 spec-sanctioned, 1 false-positive. 0 AC violations.** Re-verified green after patching. Both confirmed findings shared ONE root cause — `MaterializationPlan` had a public constructor with no invariant enforcement, so a future external caller could build an inconsistent plan AND `default(MaterializationPlan)` bypassed the ctor (null `MonsterId`, false invariants). The patch follows the `LureResult` private-ctor + factory precedent and closes the deferred finding for free.

- **[PATCHED] `MaterializationPlan` public ctor permitted inconsistent / default-bypassed plans.** Confirmed (2 findings, same root cause). Fix: made the ctor `private`, added an `internal static MaterializationPlan.Create(...)` factory so ONLY `MaterializationPresenter` (the sole tier→variant/duration/ambiance map authority) can build a plan — every plan is now internally consistent (a future external caller cannot make Tier=Common + Variant=Breach). Converted `MonsterId` to a null-safe getter (coalesces the null backing field → empty) and `HideActionBarDuringMaterialization`/`InputRecoveryBlocked` to constant PROPERTIES (not ctor-set fields), so even `default(MaterializationPlan)` honors the documented contract (no NRE on `MonsterId.Length`, the action-bar gate stays hidden). Pinned by 2 new tests (`Default_plan_monster_id_is_empty_not_null`, `Default_plan_still_hides_the_action_bar_and_never_blocks_input`). [Assets/Veilwalkers/UI/Encounter/MaterializationPlan.cs; Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs]
- **[DEFERRED — folded into the patch above] `default(MaterializationPlan)` violated the "Always true" HideActionBar invariant.** Triaged out-of-scope (the story never produces/consumes a default plan — `PlanFor` always returns a constructed one). Same root cause as the patch above, so the property-not-field fix RESOLVES it anyway: `default(MaterializationPlan).HideActionBarDuringMaterialization` is now `true`. No separate work remains. [Assets/Veilwalkers/UI/Encounter/MaterializationPlan.cs]
- **[SPEC-SANCTIONED] No test for out-of-bounds/undefined `Rarity` values; future-tier silent degradation.** (2 findings.) The presenter's `default:` cases degrade an unmapped tier to the calmest entrance (NFR-3 graceful boundary — documented at the switch sites), rather than throw at the render boundary. The anti-tautology bar mandates mutation tests on the 5 EXISTING append-only `Rarity` members, not compile-time completeness for hypothetical future tiers; callers pass a real `Rarity`, never an arbitrary cast int. A future tier addition must manually extend the switches + `AscendingTiers` (the standard append-only discipline). Owner: whoever adds a 6th `Rarity` tier. [Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs (the switch default cases)]
- **[SPEC-SANCTIONED] `MaterializationView._reducedMotion` is a stubbed `false` until Epic 6.** The reduced-motion SETTING read/persistence is the approved Epic-6 deferral (Dev Notes scope matrix); the view stubs the field and the presenter's taming LOGIC is proven by the `reducedMotion=true` tests. Owner: Epic 6 (already recorded in the 4.7 dev deferrals above). [Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (_reducedMotion)]
- **[SPEC-SANCTIONED] `Render` stub logs `{plan.Tier}` (a corrupted enum would print its number).** `Render` is the explicitly-sanctioned Epic-6 stub (diagnostic `GameLog.Info`); logging robustness for theoretically-corrupted enums belongs to the Epic-6 render layer, not the decision layer. Owner: Epic 6. [Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (Render)]
- **[FALSE-POSITIVE] `GameLog.Info` dependency unverified.** A file-blind layer flagged it as an assumption; verified real — `GameLog.Info(string)` exists in `Veilwalkers.Core/GameLog.cs:21`, `Veilwalkers.UI.asmdef` references `Veilwalkers.Core`, and the `using` is present. The view compiles + runs (the `CodexDetailView` stub-log precedent). No action.

## Deferred from: development of 4-7-materialize-monsters-with-tiered-accessible-entrances (2026-06-15)

- **The actual materialization RENDER (every pixel) is Epic 6.** 4.7 builds the materialization DECISION only — the pure-logic `MaterializationPresenter` returns a `MaterializationPlan` (per-tier `MaterializationVariant` + `DurationSeconds` + `MaterializationAmbiance` + the `BrandedGlitch`/`ReducedMotion`/`HideActionBarDuringMaterialization`/`InputRecoveryBlocked` flags). The RENDER — the actual entrance tweens (Pop-in/Unfurl/Seep/Tear/Breach), the T5 camera-feed glitch shader, the ambiance tint/lighting/vignette/SLAY-glow tokens, the screen-shake, the action-bar show/hide animation — is Epic 6 (no AR rig scene / chunky component kit / design tokens exist yet: 6.1 tokens, 6.2 components, 6.3 surfaces). 4.7 returns the plan; Epic 6 renders it. Owner: Epic 6. [Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs; Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (Render stub); UX-DR10/DR11]
- **The reduced-motion / photosensitivity SETTING + its persistence is Epic 6.** 4.7 takes reduced-motion as a presenter INPUT `bool` and proves the taming decision (no glitch/shake/strobe, entrance + ambiance preserved so the dread still reads — AC-3). The actual accessibility SETTING UI, its `SaveModel` flag / player-pref persistence, and the wiring of that setting into `MaterializationView` (currently a stubbed `_reducedMotion` field with a TODO) are Epic 6 (UX-DR17 accessibility floor). Owner: Epic 6. [Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (_reducedMotion stub)]
- **The AR-rig scene wiring that drives `MaterializationView.Play` is Epic 6 / Story 6.3.** 4.7's `MaterializationView` is inert — it constructs the presenter directly (no service-locator read, the presenter is dependency-free) and exposes `Play(monsterId, tier)` as the Epic-6 wire point, but nothing CALLS it yet (no AR rig scene, no live encounter→materialization handoff). Epic 6 reads the tier off the spawned Monster (the `LureResult` id + the Codex tier) and calls `Play` after the synchronous spend-ack. Owner: Epic 6 / Story 6.3. [Assets/Veilwalkers/UI/Encounter/MaterializationView.cs (Play); Assets/Veilwalkers/Encounter/LureResult.cs]
- **The exact per-tier entrance DURATIONS are Epic-6 / OQ-9 polish (provisional).** 4.7 pins the BEHAVIOR — durations strictly monotonic non-decreasing Common→Nightmare, each within its AC band (T1 ~0.5s … T5 ~2.5–3s) — NOT the exact ms. The values are `internal const`s on `MaterializationPresenter` (`CommonDuration` 0.5f … `NightmareDuration` 2.75f), NOT `EconomyConfig` fields (these are UX timings, not economy values — the `LureSystem.BasicRareChance` precedent). The exact timing is animation polish the Epic-6 render / an OQ-9 feel pass may retune. The tests assert the band, so retuning within the band keeps them green. Owner: Epic 6 / OQ-9 polish. [Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs (the *Duration consts)]
- **[SETTLED] The 4.2 "tiered materialization VFX / entrance render is Story 4.7 / Epic 6" deferral is resolved at the DECISION layer.** 4.2 deferred the materialization render to 4.7 (deferred-work.md, the 4.2 dev entry) and pinned the handoff: `LureResult` carries the spawned id(s) + IS the sub-second spend-ack; "the materialization plays afterward against the returned ids." 4.7 lands the DECISION (the tier→plan map + the action-bar gate + the reduced-motion taming + the NFR-2 decoupling proof) as a pure-logic presenter; the actual VFX/scene RENDER re-defers to Epic 6 (above). The NFR-2 contract is proven at the presenter level — `PlanFor` is a synchronous, no-economy, side-effect-free value call whose duration is plan DATA, never a wait, so the spend-ack (delivered earlier by 4.2's `LureResult`) is never gated by the entrance length. [Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs]
- **No Bootstrap change.** The `MaterializationPresenter` is dependency-free (takes a `Rarity`, not a service), so the `MaterializationView` constructs it directly — there is NO live registration to add and NO `EncounterService`-style seam to document (unlike the 4.1–4.6 `MonsterDatabase.asset`-blocked services). The view is wired into the AR-rig scene in Epic 6. [Assets/Veilwalkers/UI/Encounter/MaterializationView.cs]

## Deferred from: code review of 4-6-apply-encounter-charges-stability-boost-nightveil-filter (2026-06-15)

- **No deferrals.** All 4 confirmed CR findings were PATCHED in-story: (1) the duplicated `StabilityBoostEase` magnitude centralized onto `ExtrasSystem` (the single extras-tunables home); (2) the `newChargeCount` success-branch re-read replaced with the computed `priorChargeCount - 1` (matching the failure-path "never recompute" discipline); (3) the flagged re-apply-then-fault "atomicity violation" verified CORRECT-BY-DESIGN (the end state equals the post-first-apply state — the modifier the first apply committed survives; the second apply's own slices roll back) and closed with a covering test + a clarifying comment; (4) the `0.999` eased/boosted-chance clamp given a boundary test (`< 1.0` AND `> un-eased` across every Capture/Slay/Lure variant — the OQ-9-rebalance landmine guard). 5 findings were triaged spec-sanctioned (the Nightveil-boost-unreachable-until-re-Lure seam wiring, the "Scan ease" AC-prose vs Scan-has-no-roll, the `ChargeCount` defensive out-of-lock early-out read, and the re-apply coverage item which was the same as confirmed-patch #3). 0 false-positive. 381/381 EditMode green after patching (was 379 at the dev baseline; +2 CR-patch tests).

## Deferred from: development of 4-6-apply-encounter-charges-stability-boost-nightveil-filter (2026-06-15)

- **The ease-bonus / rarity-boost MAGNITUDES (`ExtrasSystem.StabilityBoostEase = 0.10`, `NightveilRarityBoost = 0.20`) are OQ-9 balancing (provisional).** 4.6 pins the BEHAVIOR — an eased Capture/Slay success chance and a boosted Lure rare chance each STRICTLY greater than the un-eased/un-boosted one (the directly mutation-testable AC, the 4.2/4.4/4.5 "the AC is the inequality, not the value" precedent), both clamped `< 1.0` (a boost is never a certainty) — NOT the values. They are local `const`s on `ExtrasSystem` (centralized by a CR patch), not `EconomyConfig` fields, for the same reason the Lure/Capture/Slay chances are. The OQ-9 pass may tune them (and may promote them into `EconomyConfig` then). Owner: OQ-9 balancing. [Assets/Veilwalkers/Encounter/ExtrasSystem.cs (StabilityBoostEase / NightveilRarityBoost)]
- **The Nightveil rarity boost is WIRED but not yet reachable in the shipped flow — it lands when the re-Lure / queued-spawn flow does.** 4.6 threads `_activeExtras.Contains(NightveilFilter)` into `LureSystem.RollMonster`/`RollMultiMonsters` (the seam, proven by an end-to-end test that drives a Lure within an active-Nightveil encounter), but in the current state machine a Lure starts from Idle and `EndEncounter` clears `_activeExtras`, so there is no production path to apply Nightveil THEN Lure again in the SAME encounter. The boost becomes observable once the deferred re-Lure / queued-spawn flow lands (a future Encounter story). The Stability Boost ease (Capture/Slay) IS reachable today (apply, then Capture/Slay in the same encounter). Owner: a future Encounter re-Lure / queued-spawn story. [Assets/Veilwalkers/Encounter/EncounterService.cs (TryLureAsync nightveilActive seam)]
- **The atmospheric visual FILTER (AC-2) is a LOGICAL flag only — the VFX/shader is Epic 6.** 4.6 records "Nightveil active" in `_activeExtras` (the render-layer flag the Epic-6 VFX will read) + pins it via `Nightveil_filter_flags_the_visual_filter_active_for_the_encounter`; the actual atmospheric shader/post-process is Epic 6. Owner: Epic 6. [Assets/Veilwalkers/Encounter/EncounterService.cs (_activeExtras)]
- **The persisted disk snapshot of the active extras (across a Shop navigation / process kill) is Story 5.4.** 4.6 holds the applied-modifier set in memory (`_activeExtras`, cleared on `EndEncounter`) — the SAME deferral posture as the 4.3 in-encounter scan-progress (`_scannedThisEncounter`). The state-faithful disk round-trip is Story 5.4. Owner: Story 5.4. [Assets/Veilwalkers/Encounter/EncounterService.cs (_activeExtras); Assets/Veilwalkers/Persistence/EncounterSnapshotData.cs]
- **The extras action-bar UI (the charge-count pills, the "earn via XP" zero-charge hint, the apply affordances) is Epic 6.** 4.6 returns the typed `ApplyExtraResult` (carrying `Kind` + `RemainingCharges` for the HUD pill) + surfaces the zero-charge block via `ApplyExtraFailureReason.InsufficientCharges`; the chunky action-bar charge-items (UX-DR12 — show a charge count, not a credit cost), the "earn via XP" hint binding, and the visual-filter treatment are Epic 6. Owner: Epic 6. [Assets/Veilwalkers/Encounter/ApplyExtraResult.cs; UX-DR12]
- **[SETTLED] The 4.1/4.4 "extras 4.6 may compose `RunActionAsync`/`CommitActionAsync`" deferral is resolved: NO (for the two non-capture extras).** `CommitActionAsync` ALWAYS stages a Codex discovery (`StageDiscovery`); an extra consumes a charge but discovers NOTHING. Routing an extra through it would require a bogus `DiscoverySource` and would discover a Monster as a side effect of a Boost. So the extras get their own composed write (`CommitApplyExtraAsync`, the SIXTH sibling — charge-only + an in-encounter `_activeExtras` modifier, no codex/XP), reusing the charge-consume slice from `CommitCaptureAsync` + the in-encounter-record from `CommitScanAsync`. Strong Capture still flows through the 4.4 Capture path. The 4.1 out-of-sequence deferral is also SETTLED for the extras path (distinct `NotSettled`, not `PersistenceFailed`+0). `RunActionAsync`/`CommitActionAsync` remain the generic charge+discovery primitive (no remaining caller in 4.6 — its section comment was updated). [Assets/Veilwalkers/Encounter/EncounterService.cs (CommitApplyExtraAsync, TryApplyExtraAsync)]

## Deferred from: code review of 4-5-slay-a-monster-for-superior-loot (2026-06-15)

- **No deferrals.** The 1 confirmed CR finding was PATCHED in-story (the `EconomyConfig.OnValidate` positive-`XpPerSlay`/`XpPerCapture` edit-time guard — a non-positive XP amount passed the FR-9 relative check yet crashed `StageXpGrant` at runtime). 6 findings were triaged spec-sanctioned (the two-state `NotSettled` recovery, the recovery-swap `priorCredits` precedent, the disjoint-fields revert-ordering claim, the roll-is-the-perform-gate RNG sequencing, the `EndEncounter()`-on-throw reconcile, the post-`CompleteResolve` `OnInsufficientCredits` raise — all matched to Decisions C/E/F + the failure-path-cleanup-parity discipline) and 1 false-positive (`SlayResult.MonsterId` populated on a miss vs the narrow field-doc). 364/364 EditMode green after patching.

## Deferred from: development of 4-5-slay-a-monster-for-superior-loot (2026-06-15)

- **"Strictly superior loot" is MORE XP + the `Slain` flag ONLY — the materials/variant/loot-drop reward channel is a future story (Decision B).** epics.md:695 phrases the reward as "materials and/or XP and/or variant" — the "and/or" lets XP-superiority alone satisfy AC-2, which 4.5 does (`XpPerSlay` 25 > `XpPerCapture` 10, pinned both as the direct inequality and behaviorally). The codebase has NO materials inventory, NO variant-drop mechanic, and NO loot-table anywhere, so 4.5 does NOT build one — it would be net-new systems with no consuming UI (the reward render is Epic 6 regardless). A Slay materials/variant-drop reward channel (a loot table keyed by Monster rarity, a materials currency in `SaveModel`, the variant-flag drop on the `CodexEntryData.VariantFlags` that already exists) is a future gameplay/economy story. Owner: a future loot-table / reward-channel story (or the OQ-9 balancing pass if it scopes materials). [Assets/Veilwalkers/Encounter/SlaySystem.cs; docs/epics.md:695]
- **The Slay success PROBABILITY (`SlaySystem.SlaySuccessChance = 0.65`) is OQ-9 balancing (provisional).** 4.5 pins the BEHAVIOR (a failed Slay leaves the encounter live + free Retry, no Credits lost; rewards superior to Capture), NOT the success value — exactly as 4.2's `LureSystem.BasicRareChance`/`PremiumRareChance` and 4.4's `CaptureSystem.BaseCaptureChance`/`StrongCaptureChance` are local `const`s, not `EconomyConfig` fields (the AC is the behavior, not the number; adding an SO field widens `EconomyConfig.SetForTests`). The OQ-9 pass may tune it (and may promote it into `EconomyConfig` then). 0.65 was chosen as "light combat" between base-Capture 0.50 and Strong-Capture 0.85 — arbitrary within that intent. Owner: OQ-9 balancing. [Assets/Veilwalkers/Encounter/SlaySystem.cs (SlaySuccessChance)]
- **The Slay/loot UI + the materialize-defeat render are Epic 6.** 4.5 returns the typed `SlayResult` (carrying the slain Monster id, the roll outcome, the cost for the HUD "Slay − 3" affordance, the post-spend balance); the chunky action-bar Slay affordance (UX-DR12), the defeat/loot animation, the "slain — Retry" vs "can't — not enough Credits" HUD treatments, and the binding of `OnMonsterDiscovered`/`OnInsufficientCredits` to the HUD/top-up sheet (UX-DR13) are Epic 6. Owner: Epic 6. [Assets/Veilwalkers/Encounter/SlayResult.cs; UX-DR12, UX-DR13]
- **No live EncounterService Bootstrap registration (inherited `MonsterDatabase.asset` blocker).** Same posture as 4.1–4.4 — `TrySlayAsync`/`SlaySystem` are fully proven headless by `Veilwalkers.Encounter.Tests`; the live registration (constructing `SlaySystem` over the live `IRandom`/`EconomyConfig` + passing it to the `EncounterService` ctor) closes when the `.asset` + CodexService registration land. The Bootstrap seam comment now lists the `slaySystem` ctor param. Owner: the Editor-authoring session closing the 2.2 `.asset` deferral + Bootstrap wiring. [Assets/Veilwalkers/App/Bootstrap.cs (EncounterService seam)]
- **[SETTLED] The 4.4 "Slay decides whether to fold `StageXpGrant` into `RunActionAsync`/`CommitActionAsync`" deferral is resolved: NO.** `CommitActionAsync` consumes a CHARGE + grants NO XP — the wrong delta for Slay (credits + codex + XP). Slay gets its own composed write (`CommitSlayAsync`, the FIFTH sibling), reusing the credit-spend slice shape from `CommitLureSpendAsync` + the discovery+XP slice shape from `CommitCaptureAsync`'s winning branch. `RunActionAsync`/`CommitActionAsync` remain the generic charge+discovery primitive (extras 4.6 may compose it). The 4.1 out-of-sequence deferral is also SETTLED for Slay (distinct `NotSettled`, not `PersistenceFailed`+0), and the 4.1/4.2 "Slay is the second credit-spending caller of `OnInsufficientCredits`" deferral is SETTLED (it is). [Assets/Veilwalkers/Encounter/EncounterService.cs (CommitSlayAsync, TrySlayAsync)]

## Deferred from: code review of 4-4-capture-a-monster-free-base-strong-capture (2026-06-15)

- **No deferrals.** All 5 confirmed CR findings were PATCHED in-story (the duplicate StrongCapture event, the RequireModel-strands-Resolving reconcile, the chargeConsumed-flag-before-SetCount, the load-bearing revert-order comment, the hardcoded `wasStrong`). 22 findings were triaged spec-sanctioned (AC/decision conformance) and 8 false-positive. 353/353 EditMode green after patching.

## Deferred from: development of 4-4-capture-a-monster-free-base-strong-capture (2026-06-15)

- **The Capture success PROBABILITY numbers are OQ-9 balancing (provisional).** 4.4 pins the STRICT `StrongCaptureChance > BaseCaptureChance` invariant (the AC-2 requirement); the actual percentages (`CaptureSystem.BaseCaptureChance = 0.50`, `StrongCaptureChance = 0.85`) are placeholder tunables. They are local `const`s on `CaptureSystem` rather than `EconomyConfig` fields — the SAME decision (and reasoning) as 4.2's `LureSystem.BasicRareChance`/`PremiumRareChance` (deferred-work.md, the 4.2 entry): the AC is the inequality, not the values, and adding two SO fields would widen `EconomyConfig.SetForTests` (currently 13 args) + need another `InternalsVisibleTo`. The OQ-9 pass may promote them into `EconomyConfig` when it tunes them. Owner: OQ-9 balancing. [Assets/Veilwalkers/Encounter/CaptureSystem.cs (BaseCaptureChance/StrongCaptureChance)]
- **Whether a failed Strong roll REFUNDS the charge is settled as "consumed on the attempt" (Decision A) — a future OQ-9 balancing change could flip it.** 4.4 reads epics.md:31 ("consumes one charge to RAISE SUCCESS") + architecture.md:77 ("using an extra consumes one charge") as: the charge buys the improved ODDS, so it is spent on the attempt win-or-lose (pinned by `Strong_capture_MISS_still_consumes_the_charge_*`). If product/balancing later wants charge-refund-on-miss (a "you only pay if it works" feel), that is an OQ-9 change owning the test flip + the `CommitCaptureAsync` consume-vs-refund branch. Owner: OQ-9 balancing / a PM decision, if ever wanted. [Assets/Veilwalkers/Encounter/EncounterService.cs (CommitCaptureAsync, Decision A)]
- **The Capture/materialization/reveal UI is Epic 6.** 4.4 returns the typed `CaptureResult` (carrying the captured Monster id, the roll outcome, the remaining charges for the HUD pill); the chunky action-bar Capture affordance (free base / Strong variant — UX-DR12), the capture/reveal animation, the "missed — Retry" vs "can't — no charge" HUD treatments, and the binding of `OnMonsterDiscovered`/`OnChargesChanged` to the HUD are Epic 6. Owner: Epic 6. [Assets/Veilwalkers/Encounter/CaptureResult.cs; UX-DR12]
- **No live EncounterService Bootstrap registration (inherited `MonsterDatabase.asset` blocker).** Same posture as 4.1/4.2/4.3 — `TryCaptureAsync`/`CaptureSystem`/`StageXpGrant` are fully proven headless by `Veilwalkers.Encounter.Tests`; the live registration (constructing `CaptureSystem` over the live `IRandom` + passing it to the `EncounterService` ctor) closes when the `.asset` + CodexService registration land. The Bootstrap seam comment now lists the `captureSystem` ctor param. Owner: the Editor-authoring session closing the 2.2 `.asset` deferral + Bootstrap wiring. [Assets/Veilwalkers/App/Bootstrap.cs (EncounterService seam)]
- **`RunActionAsync`/`CommitActionAsync` (the generic charge+discovery primitive) is retained for Slay (4.5) — but Slay does NOT roll/grant-XP through it yet.** 4.4 replaced the 4.1 representative Capture (`TryCaptureAsync(id)`/`TryCaptureInEncounterAsync`) with the real rolled Capture and kept `RunActionAsync` as the generic primitive (now covered by `RunAction_*` pins). Slay (4.5) will compose it — and Slay ALSO grants XP (more than Capture, FR-9), so 4.5 owns deciding whether to fold `StageXpGrant` into `RunActionAsync`/`CommitActionAsync` (as 4.4 did for Capture) or add a Slay-specific composed write. Owner: Story 4.5. [Assets/Veilwalkers/Encounter/EncounterService.cs (RunActionAsync/CommitActionAsync)]

## Deferred from: code review of 4-3-scan-a-monster-for-free (2026-06-14)

- **`ScanResult.Succeeded(monsterId, alreadyScanned)` does not guard a null/empty `monsterId`** — the ctor coalesces null → `string.Empty`, and (unlike `Failed`, which throws on a `None` reason) `Succeeded` does not enforce that a successful scan carries a real id. Triaged **spec-sanctioned**: Decision D pins validation at the public `TryScanAsync` entry point (which throws on a null/empty/invalid id before any factory call), the established `LureResult.Succeeded` precedent — the internal factory is only ever reached after validation, so re-validating would duplicate the guard for no architectural benefit. Owner: a future test-infra / factory-hardening pass, if a second caller of `ScanResult.Succeeded` ever bypasses the entry-point validation. [Assets/Veilwalkers/Encounter/ScanResult.cs (Succeeded factory)]
- **A first scan THIS encounter of a Monster already scanned in a PRIOR encounter writes an unchanged `SaveModel` (one `SaveAsync`)** — when the persistent `Scanned` flag is already true (prior encounter) but the Monster is not yet in `_scannedThisEncounter`, the no-op skip (which requires `!stage.Applied` AND `alreadyScannedThisEncounter`) is not taken, so `CommitScanAsync` persists once even though the durable `SaveModel` is unchanged (only the in-memory encounter-state changed). Triaged **spec-sanctioned**: AC-2 explicitly says "a re-scan that WOULD change the encounter-state … still persists once" — the encounter-state DID change (the Decision-B1 record), and the single `SaveAsync` is also what drives the recovery-swap/fault rollback guard; AR-8 (one persist per action) is honored. A covering test was ADDED (`Scan_of_a_monster_scanned_in_a_prior_encounter_persists_once_and_is_not_already_scanned`) to close the flagged test gap. An optimization that skips the persist when ONLY the in-memory encounter-state changed (no durable delta) is a possible future micro-optimization, but it would forgo the rollback-guard's `SaveAsync` and is not warranted at the real call site. Owner: a future Encounter persist-optimization pass, if ever wanted. [Assets/Veilwalkers/Encounter/EncounterService.cs (CommitScanAsync no-op skip)]

## Deferred from: development of 4-3-scan-a-monster-for-free (2026-06-14)

- **The persisted scan-progress snapshot/rehydrate (the disk round-trip of the in-encounter scan state) is Story 5.4.** 4.3 records the in-encounter scan progress in memory (`EncounterService._scannedThisEncounter`, the 4.1 `_activeAnchors` precedent) + the persistent `CodexEntryData.Scanned` flag on already-discovered Monsters; the state-faithful DISK snapshot of per-monster scan progress across a Shop navigation / process kill (architecture.md:475 — the `EncounterMonsterStateData.ScanProgress` DTO already exists) is Story 5.4. 4.3 deliberately does NOT build the `EncounterSnapshotData` round-trip. Owner: Story 5.4. [Assets/Veilwalkers/Persistence/EncounterMonsterStateData.cs (ScanProgress); Assets/Veilwalkers/Encounter/EncounterService.cs (_scannedThisEncounter)]
- **The Scan reveal UI (the revealed-info render + the partial-Codex reveal animation) is Epic 6.** 4.3 returns the typed `ScanResult` (carrying the scanned Monster id) + records the flag; the chunky HUD reveal of the scanned info, the silhouette→partial-art reveal (OQ-7 hybrid gradual reveal, UX-DR15), and the action-bar Scan affordance (UX-DR12) are Epic 6. Owner: Epic 6. [Assets/Veilwalkers/Encounter/ScanResult.cs; UX-DR12, UX-DR15]
- **No live EncounterService/CodexService Bootstrap registration (inherited `MonsterDatabase.asset` blocker).** Same posture as 4.1/4.2 — `TryScanAsync`/`StageScan` are fully proven headless by `Veilwalkers.Encounter.Tests` / `Veilwalkers.Monsters.Tests`; the live registration closes when the `.asset` + CodexService registration land. Owner: the Editor-authoring session closing the 2.2 `.asset` deferral + Bootstrap wiring. [Assets/Veilwalkers/App/Bootstrap.cs (EncounterService/CodexService seam)]
- **The scan-vs-discovery count split is settled by the encounter-state model, NOT by re-defining `DiscoveredCount` (Decision B1).** 4.3 keeps "key-presence == discovered == X/67" inviolate: an undiscovered Monster's Scan records progress in the encounter state and `CodexService.StageScan` NEVER creates a Codex key, so a scan-only Monster never appears in X/67 and never raises `OnMonsterDiscovered`. If a future story ever wants scan-progress to PERSIST in the Codex independently of discovery (Model B2 — a scan-only key, with `DiscoveredCount` redefined to count only Capture/Slay entries), it owns the count-split + the Epic-2 grid/count regression. Owner: a future codex-model story / OQ balancing, if ever wanted. [Assets/Veilwalkers/Monsters/Codex/CodexService.cs (StageScan, DiscoveredCount)]
- **`ScanProgress` is a boolean-grained "scanned this encounter" in 4.3, not a fractional [0,1] progress.** The `EncounterMonsterStateData.ScanProgress` DTO is a `float` in [0,1] (for a future multi-tick scan), but 4.3's encounter scan-state is a simple per-monster "scanned" set (one Scan completes it) — the AC is "information is revealed / partial data recorded," not a timed/graded scan. A fractional scan-progress mechanic (if balancing ever wants a multi-second hold-to-scan) is a future gameplay/balancing story that would populate the `float`. Owner: a future scan-mechanic / OQ balancing story. [Assets/Veilwalkers/Encounter/EncounterService.cs (_scannedThisEncounter); Assets/Veilwalkers/Persistence/EncounterMonsterStateData.cs (ScanProgress float)]

## Deferred from: code review of 4-2-lure-a-monster-basic-premium-multi (2026-06-14)

- **`RefundLureSpendAsync` has no overflow guard on the credit re-credit (`model.Credits += cost`).** The spend path is guarded (`priorCredits < cost` rejects before deducting), but the refund path adds `cost` back without a `checked`/overflow guard — a balance near `int.MaxValue` could silently wrap. Triaged **out-of-scope for 4.2**: this matches the already-recorded "refund-path + concurrency hardening" dev deferral; realistically credit balances are bounded by purchase packs (~hundreds of thousands) far below `int.MaxValue` (~2.1B), so it is a safety edge-case, not a breaking defect, and the refund path itself is the NFR-3 graceful path. Owner: a future Economy/Encounter overflow-hardening pass (centralize a `checked` credit mutator across CreditService/DailyReward/Encounter). [Assets/Veilwalkers/Encounter/EncounterService.cs (RefundLureSpendAsync)]
- **No zero-cost Lure boundary test.** The exact-balance boundary (balance == cost) is pinned (`Exact_balance_lure_succeeds_to_zero`), but a cost of 0 is not tested. Triaged **out-of-scope for 4.2**: Lure costs are non-zero canon configuration (Basic 1 / Premium 4 / Multi 5 via `EconomyConfig`), with no dynamic path that nullifies a cost; a zero-cost guard/validator is an `EconomyConfig`-validation concern, not 4.2's AC-1–4 scope. Owner: a future config-validation / robustness story (or the OQ-9 balancing pass if it ever introduces a free Lure). [Assets/Tests/EditMode/Encounter.Tests/EncounterServiceTests.cs]

## Deferred from: development of 4-2-lure-a-monster-basic-premium-multi (2026-06-14)

- **[SETTLED in Story 4.7 at the DECISION layer; render re-deferred to Epic 6] The tiered materialization VFX / entrance render (T1 Pop-in … T5 Breach) is Story 4.7 / Epic 6.** Resolved 2026-06-15: Story 4.7 landed the materialization DECISION as a pure-logic `MaterializationPresenter` (the tier→variant+duration map, the action-bar-hidden gate, the T5 branded-glitch flag, the reduced-motion taming, the NFR-2 spend-ack decoupling proof — `PlanFor` is synchronous + does no economy work, so the entrance duration never gates the ack). The actual VFX/scene RENDER (tweens, the camera glitch shader, ambiance tokens, screen-shake, the action-bar animation, the AR-rig scene wiring, the reduced-motion SETTING + persistence) re-defers to Epic 6 (see the 4.7 dev entry at the top). (Original note below.) 4.2 does the LOGICAL spawn — `MonsterSpawner.TrySpawn` (pool activate) + the `AnchorToken` placement + the `Idle → Lured` transition — and returns the spawned Monster id(s) on the `LureResult`. The cinematic entrance (UX-DR10), the action-bar-hidden-during-materialization (UX-DR12), the T5 Breach branded glitch (UX-DR11), and the spend-ack-vs-materialization-duration animation (NFR-2) are Story 4.7. 4.2's `LureResult` IS the sub-second spend-ack contract (returned synchronously after the persist commits); the materialization plays afterward against the returned ids. Owner: Story 4.7 / Epic 6. [Assets/Veilwalkers/Encounter/EncounterService.cs (TryLureAsync, ActivateSpawns); Assets/Veilwalkers/UI/Encounter/MaterializationPresenter.cs]
- **The Guaranteed-Rare Lure (forced `Rarity >= Rare`) is Story 5.3.** 4.2 builds Basic/Premium/Multi with PROBABILISTIC rare chance (Premium STRICTLY > Basic — `LureSystem.PremiumRareChance` > `BasicRareChance`, pinned). The one-shot Guaranteed-Rare item that FORCES the tier floor (granted by `PurchaseReconciler`, consumed through the Lure path with the `Rarity >= RarityThresholds.GuaranteedRareFloor` filter) is Story 5.3. The `LureKind` enum deliberately leaves room for a `GuaranteedRare` kind (documented on the enum). Owner: Story 5.3. [Assets/Veilwalkers/Encounter/LureKind.cs; Assets/Veilwalkers/Encounter/LureSystem.cs; docs/architecture.md:692-702]
- **The exact rare-chance probability NUMBERS are OQ-9 / OQ-2 balancing (provisional).** 4.2 pins the STRICT Premium>Basic inequality (the AC-2 invariant); the actual percentages (`LureSystem.BasicRareChance = 0.15`, `PremiumRareChance = 0.50`) are placeholder tunables. They are local `const`s on `LureSystem` rather than `EconomyConfig` fields for now — the decision: the AC is the inequality, not the values, and adding two SO fields would widen `EconomyConfig.SetForTests` (13 args) + need a third `InternalsVisibleTo`; the OQ-9 pass may promote them into `EconomyConfig` when it tunes them. Multi-Lure rolls each monster at the Basic chance (a volume play, not a rarity play — revisitable by balancing). Owner: OQ-9 / OQ-2 balancing. [Assets/Veilwalkers/Encounter/LureSystem.cs (BasicRareChance/PremiumRareChance)]
- **No live EncounterService / LureSystem Bootstrap registration (inherited `MonsterDatabase.asset` blocker).** Same posture as 4.1 — both need a `MonsterDatabase` whose `.asset` is unauthored (Story 2.2 `[~]`): `LureSystem` for the rarity roll, `EncounterService` for the `CodexService` built over it. The Bootstrap seam comment now lists the Story 4.2 collaborators (LureSystem + the live `planeAnchorService`/`monsterSpawner`); `IRandom`/`SystemRandom` IS constructed + registered live (no blocker). The seam closes when the `.asset` + CodexService land. The Lure logic is fully proven headless by `Veilwalkers.Encounter.Tests`. Owner: the Editor-authoring session closing the 2.2 `.asset` deferral + Bootstrap wiring. [Assets/Veilwalkers/App/Bootstrap.cs (EncounterService/LureSystem seam)]
- **The credit-pill spend-ack UI + the non-blocking top-up sheet binding are Epic 6.** 4.2 returns the typed `LureResult` (carrying the new balance for the sub-second ack) + raises `OnInsufficientCredits`; the chunky credit-pill confirmation (UX-DR4) and the top-up sheet over the live encounter that subscribes to the event (UX-DR13) are Epic 6 (the AR-HUD + Shop). Owner: Epic 6. [UX-DR4, UX-DR13]
- **The post-deduction spawn-failure refund is reachable but not expected at the real call site.** `TryLureAsync` pre-checks placement BEFORE the deduction (Decision E), so a spawn refusal after the persist should not occur (the placement that was secured maps to a pooled slot under the cap). The refund path (`RefundLureSpendAsync`) exists for NFR-3 completeness (a cap refusal between secure + activate). It is logic-exercised indirectly but has no dedicated cap-exhaustion test (the harness spawner cap is the default 8, far above the 1–2 a Lure activates). Owner: a future Encounter robustness pass, if a tighter cap or a concurrent spawner consumer makes the path reachable in practice. [Assets/Veilwalkers/Encounter/EncounterService.cs (RefundLureSpendAsync)]
- **Cross-service concurrency stress of the Lure credit-spend write remains a future hardening pass.** Like 4.1, 4.2 proves the atomic credit-spend + rollback + recovery-swap guard single-threadedly. A real concurrent-mutation stress harness (a Lure spend racing a plain credit spend / a composed Capture under the shared lock) is the still-deferred Economy/Encounter concurrency pass. Owner: a future concurrency-test pass. [Assets/Veilwalkers/Encounter/EncounterService.cs (CommitLureSpendAsync)]

## Deferred from: code review of 4-1-drive-the-encounter-with-an-explicit-state-machine (2026-06-14)

- **Multi-anchor recovery has no all-or-nothing atomicity.** `EncounterService.Recover()` loops over every active anchor calling `AnchorRestoreService.TryRestore`. If anchor #1 succeeds/relocates (with side effects — re-acquire / a chosen relocation plane) and a later anchor `Failed`, the method returns `Failed` and stays `Suspended`, but anchor #1's side effects are never rolled back, and a later `Recover()` re-runs `TryRestore` on the already-restored anchor (a partially-relocated encounter held Suspended with inconsistent anchor state). Triaged **out-of-scope for 4.1**: the story builds the in-memory recovery DECISION + the state-machine wiring; the actual scene re-anchor (where the side effects materialize and where all-or-nothing atomicity would be enforced) is Story 6.3 (`out Pose` is currently discarded — `out _`), and the persisted-recovery semantics are Story 5.4. AC-3 only mandates "any `Failed` → stay Suspended" (correctly implemented), not transactional multi-anchor atomicity. Owner: Story 6.3 (consistent scene re-anchor on recovery) / a recovery-hardening pass. [Assets/Veilwalkers/Encounter/EncounterService.cs (Recover, the per-anchor loop)]
- **`RunActionAsync` reports a state-machine sequencing error as `SpendFailureReason.PersistenceFailed` with charge count 0.** When the caller is not in `Acting`, `BeginResolve()` fails and `RunActionAsync` returns `SpendResult.Failed(PersistenceFailed, 0)` — a misleading reason (nothing was persisted) + a 0 count (not the caller's real balance). Triaged **out-of-scope for 4.1**: the docstring marks "must be in `Acting`" as a precondition (programmer error); the representative actions (`TryCaptureAsync`/`TryCaptureInEncounterAsync`) call `RunActionAsync` from the correct state, so the AC pins don't exercise this path, and the real per-action callers (4.2–4.6) own their precondition/error handling. Owner: Stories 4.2–4.6 — when the real callers land, give an out-of-sequence call a distinct reason (or throw, consistent with the invalid-id handling) and report the real balance. **[PARTIALLY SETTLED in Story 4.3 for the Scan path: `TryScanAsync` does NOT go through `RunActionAsync`; an out-of-sequence Scan (no settled Monster / not in `Lured`) returns the distinct `ScanFailureReason.NotSettled` — NOT the misleading `PersistenceFailed`+0 — and persists nothing (pinned by `Scan_out_of_a_live_encounter_is_a_typed_NotSettled_failure_that_persists_nothing`). The shared `RunActionAsync` BeginResolve-failed branch (used by the charge/Capture/Slay path) still reports `PersistenceFailed`+0; Capture (4.4) / Slay (4.5) / extras (4.6) own giving it a distinct reason when they land.]** [Assets/Veilwalkers/Encounter/EncounterService.cs (RunActionAsync, the BeginResolve-failed branch)]

## Deferred from: development of 4-1-drive-the-encounter-with-an-explicit-state-machine (2026-06-14)

- **The FR-6–10 per-action BUSINESS LOGIC is Stories 4.2–4.6.** 4.1 built the encounter spine: the `EncounterStateMachine` (Idle→Lured→Acting→Resolving→Resolved + Suspended), the AC-2 atomic multi-delta write PRIMITIVE (`EncounterService.CommitActionAsync` — charge AND codex in ONE `SaveAsync` under the shared `SaveMutationLock`, whole-mutation rollback, recovery-swap guard) proven by a representative composed action (`TryCaptureAsync`/`TryCaptureInEncounterAsync` — a Strong-Capture charge + a Codex Capture), and the AC-3 Suspended/recovery wiring. The actual per-action gameplay — the Lure cost-table + rarity roll (4.2), Scan reveal (4.3), Capture success math + the free-base-vs-Strong choice (4.4), Slay loot (4.5), the extras/Retry (4.6) — is downstream; those stories add public methods composing the proven `RunActionAsync`/`CommitActionAsync` primitive. Owner: Stories 4.2–4.6. **[PARTIALLY SETTLED in Story 4.2: the LURE business logic (cost table + rarity roll + placement + spawn) landed — `EncounterService.TryLureAsync` composes a credit-spend SIBLING of `CommitActionAsync` (`CommitLureSpendAsync`, credits not charges, no codex stage) + `LureSystem` (the roll) + the Epic-3 placement/spawn seams. Scan (4.3), Capture math (4.4), Slay (4.5), extras/Retry (4.6) remain.]** **[FURTHER SETTLED in Story 4.3: the SCAN business logic landed — `EncounterService.TryScanAsync` composes a FREE (no-credit, no-charge) THIRD sibling (`CommitScanAsync`) of the atomic-write pipeline + `CodexService.StageScan` (the discovered-only `Scanned`-flag staged delta that NEVER creates a key / raises no discovery event — scan ≠ discover, Decision B1). Capture math (4.4), Slay (4.5), extras/Retry (4.6) remain.]** [Assets/Veilwalkers/Encounter/EncounterService.cs]
- **The persisted Shop-round-trip snapshot/rehydrate (UJ-2) is Story 5.4.** 4.1 built the IN-MEMORY Suspended/recover wiring (the active encounter's anchors are held in memory + restored via `AnchorRestoreService.TryRestore` on `Recover()`). The behavior-bearing `EncounterSnapshot` (Encounter tier) ↔ `EncounterSnapshotData` (Persistence DTO) DISK round-trip across a Shop navigation / process kill — state-faithful (scan progress, applied boosts), Safety-Warning-does-not-refire — is Story 5.4. 4.1 did NOT build the disk snapshot/rehydrate or the `EncounterSnapshot` behavior object (the in-memory `_activeAnchors` array suffices for AC-3). Owner: Story 5.4. [Assets/Veilwalkers/Persistence/EncounterSnapshotData.cs; Assets/Veilwalkers/Encounter/EncounterService.cs (_activeAnchors)]
- **The actual scene re-anchor + materialization render is Story 6.3 / Epic 6.** 4.1's `Recover()` DECIDES the outcome (`Restored`/`RelocatedToPlane` resume; `Failed` stays Suspended) + drives the state machine; the actual GameObject re-parent/re-anchor to the `TryRestore` pose, and the "pulled back through the veil" Lure-VFX beat (no Lure/materialization VFX exists yet), are the not-yet-placed AR rig (6.3) / the materialization primitive (Epic 6). The `out Pose` from `TryRestore` is currently discarded in `Recover()` (`out _`) — the scene wiring (6.3) reads it to actually move the object. Owner: Story 6.3 / Epic 6. [Assets/Veilwalkers/Encounter/EncounterService.cs (Recover, out _)]
- **EncounterService Bootstrap registration is deferred on the same `MonsterDatabase.asset` blocker as CodexService (decision J1).** EncounterService needs CodexService, which is unregistered until `MonsterDatabase.asset` is authored (the Story 2.2 `[~]` deferral). A documented registration SEAM (mirroring the CodexService seam) is in `Bootstrap.cs`; the Encounter logic is fully proven headless by `Veilwalkers.Encounter.Tests` regardless. The live registration (passing the SAME `economyMutationLock` — load-bearing) closes when the `.asset` + CodexService land. Owner: the Editor-authoring session closing the 2.2 `.asset` deferral + Bootstrap wiring. [Assets/Veilwalkers/App/Bootstrap.cs (EncounterService seam)]
- **[SETTLED in Story 4.2] The OnInsufficientCredits surfacing seam exists but has no credit-spending caller yet (4.2/4.5).** Resolved 2026-06-14: Story 4.2's `EncounterService.TryLureAsync` is the FIRST credit-spending caller — on a balance-too-low Lure it raises `OnInsufficientCredits` (via `RaiseInsufficientCredits`) with `InsufficientCreditsEvent(cost, balance)` and never opens the Shop (AR-11). Pinned by `EncounterServiceTests.Insufficient_credits_blocks_raises_event_and_persists_nothing`. What REMAINS: Slay (4.5) is the second credit-spending caller; Epic 6 binds the event to the non-blocking top-up sheet (UX-DR13). (Original note below.) 4.1's representative action consumes a CHARGE (no credit spend), so `EncounterService.OnInsufficientCredits`/`RaiseInsufficientCredits` is the AR-11 seam (services never open the Shop) but is exercised only once the credit-spending Lure (4.2) / Slay (4.5) actions land. The `RaiseChargesChanged` helper is likewise a logging seam — the authoritative charge-change event is on `IProgressionService` (not re-raisable from outside); the composed write mutates charges WITHOUT going through ProgressionService (it owns the single persist), so Epic 6's HUD binds the encounter action RESULT directly rather than the progression event. Owner: Stories 4.2/4.5 (credit spends) + Epic 6 (HUD binding). [Assets/Veilwalkers/Encounter/EncounterService.cs (OnInsufficientCredits, RaiseChargesChanged)]
- **Decision-H2 (suspend-during-Resolving) is proven at the state-machine level, not via an async-interleave service test.** The deferred-suspend-during-Resolving rule is pinned by `EncounterStateMachineTests.Suspend_during_Resolving_is_deferred_until_the_write_settles` (the decision logic). A SERVICE-level test that raises `OnArSessionInterrupted` WHILE `CommitActionAsync`'s `SaveAsync` is mid-await would need a save-gate to deterministically interleave the async write with the synchronous interruption — added complexity for marginal coverage over the state-machine pin. Owner: a future Encounter concurrency-test pass (or whenever a save-gate harness lands for the composed write). [Assets/Tests/EditMode/Encounter.Tests/EncounterStateMachineTests.cs]
- **Cross-service concurrency stress of the composed write remains a future hardening pass.** 4.1 proves the atomic write + whole-mutation rollback + recovery-swap guard single-threadedly (the Economy/Codex test precedent). A real concurrent-mutation stress harness (the composed Encounter write racing a plain credit spend under the shared lock) is the deferred Economy concurrency-hardening pass several prior CRs named (1.4/1.5/1.8/2.3) — 4.1 now EXERCISES the shared-lock mechanism with a composed multi-delta writer (the widest real consumer), but does not stress it under contention. Owner: a future Economy/Encounter concurrency-test pass. [Assets/Veilwalkers/Encounter/EncounterService.cs]

## Deferred from: code review of 3-5-save-and-restore-anchors-across-interruption (2026-06-14)

- **`FakeArAnchorProvider.TryGetRelocationCandidates` returns its internal array reference (no defensive copy)** — the fake hands back `candidates = RelocationCandidates` (the same array it holds). No bug today (the production `AnchorRestoreService` only READS the array — it never sorts/mutates), but a future service that mutates candidates in place would silently corrupt the fake's seed state, and the real device adapter returns a fresh array each call (so the fake's contract is subtly weaker than production). Triaged **out-of-scope for 3.5**: the `IArAnchorProvider.TryGetRelocationCandidates` seam contract does not mandate a defensive copy, and the anti-tautology bar asserts the typed result + seam call counts (not mutation safety). Owner: a future test-infra / adapter-contract pass (or whenever a consumer mutates the candidate array). [Assets/Tests/EditMode/AR.Tests/FakeArAnchorProvider.cs]
- **`TryReacquireAnchor` success pose is not validated for a non-zero (valid) rotation** — when re-acquire succeeds, `AnchorRestoreService` returns the provider's pose directly without checking the quaternion is non-degenerate. Triaged **out-of-scope for 3.5**: pose VALIDITY is the thin adapter's contract (AR-4 — the service trusts the seam; the seam is the device glue), and the device re-acquire path is `TODO(Story 6.3)`. The fake always returns `Quaternion.identity`. Owner: Story 6.3 (the device `ArcoreAnchorProvider.TryReacquireAnchor` must return a valid pose / the service could add a guard then if needed). [Assets/Veilwalkers/AR/AnchorRestoreService.cs; Assets/Veilwalkers/AR/ArcoreAnchorProvider.cs (device path)]
- **Tie-breaking among multiple in-frustum planes at identical `DistanceFromCamera` is undefined (first-seen wins)** — `SelectRelocationTarget` uses strict `<`, so on a distance tie the first-seen candidate wins. Triaged **out-of-scope for 3.5**: this is deterministic and correct for AC-3 (ANY nearest in-frustum plane satisfies "nearest plane within the current camera frustum"); a smarter tie-break (e.g. most-centered-in-view, largest plane) is a balancing/UX refinement, not an AC. Owner: a future relocation-tuning / UX pass. [Assets/Veilwalkers/AR/AnchorRestoreService.cs (SelectRelocationTarget)]
- **NaN/Infinity `DistanceFromCamera` robustness (RESOLVED as a patch, noted here for completeness)** — one review layer flagged the non-finite-distance trap as a defer; it was triaged as a confirmed PATCH instead (the selection is the pure-logic decision 3.5 owns, so the guard belongs here, not deferred to the adapter). `SelectRelocationTarget` now skips non-finite distances (a NaN can never win the ranking) and falls back to the first candidate if all are non-finite. Pinned by `Lost_anchor_ignores_a_NaN_distance_candidate_...`. No further work. [Assets/Veilwalkers/AR/AnchorRestoreService.cs]

## Deferred from: development of 3-5-save-and-restore-anchors-across-interruption (2026-06-14)

- **`ArcoreAnchorProvider` restore device-glue is a logic-complete-but-subsystem-stub adapter (sanctioned scope).** The AR Foundation anchor re-acquisition (re-resolve the saved `TrackableId`) + the `ARPlaneManager`-against-camera-frustum candidate query are scene-and-device-only + CI-untestable (architecture.md:591). The `#else` editor path is real (re-acquire fails / no candidates → the restore DECISION is reachable in-editor on its Failed path and proven against `FakeArAnchorProvider`); the `#if UNITY_ANDROID && !UNITY_EDITOR` device path is `TODO(Story 6.3)` wired when the AR rig scene lands. Owner: Story 6.3. [Assets/Veilwalkers/AR/ArcoreAnchorProvider.cs (restore device-path)]
- **[SETTLED in Story 4.1] The `EncounterStateMachine.Suspended` consumer + the recovery CALL SITE (calling `AnchorRestoreService.TryRestore` on recovery) are Epic 4 (deliberate altitude split).** Resolved 2026-06-14: Story 4.1 built the `EncounterStateMachine` with the `Suspended` state and `EncounterService` which (a) subscribes to `ArSessionService.OnArSessionInterrupted` (raised by 3.3) → enters `Suspended` (an interruption during the atomic-write window is deferred — decision H2) + surfaces guidance via `OnStateChanged`, and (b) on `Recover()` CALLS `AnchorRestoreService.TryRestore` (the 3.5 decision) per active anchor: `Restored`/`RelocatedToPlane` resume the encounter, `Failed` stays Suspended (never vanish, NFR-3). Pinned by `EncounterServiceTests` (Active_encounter_suspends_on_a_real_AR_interruption / Recovery_reacquires_and_resumes / Recovery_relocates / Recovery_stays_suspended_when_no_anchor_can_be_restored). What REMAINS (re-pointed to Story 6.3 / Epic 6): the actual scene GameObject re-anchor to the restored pose (the `out Pose` is currently discarded in `Recover()`), and the "pulled back through the veil" Lure-VFX beat (no VFX exists). (Original note below.) The encounter-side handling — the `Suspended` state that pauses the encounter + shows guidance, and the code that CALLS `TryRestore` on session recovery — is Epic 4. The decision exists + is tested; its caller is downstream. Owner: Epic 4. [Assets/Veilwalkers/Encounter/EncounterService.cs; Assets/Veilwalkers/Encounter/EncounterStateMachine.cs; architecture.md:650-652]
- **The "pulled back through the veil" relocation beat (reusing Lure materialization VFX) is Epic 4/6 (no Lure VFX exists yet).** AC-3's diegetic beat reuses the Lure materialization VFX (architecture.md:472-473) — but there is no Lure/materialization VFX in the project yet (Epic 4 builds Lure; Epic 6 the materialization primitive). 3.5 lands the relocation DECISION (`RelocatedToPlane` to the nearest-in-frustum plane + the chosen pose, never vanish) which CI proves; the VFX beat is the render layer, deferred. Owner: Epic 4 (Lure VFX) / Epic 6 (materialization primitive). [docs/architecture.md:472-473; Assets/Veilwalkers/AR/AnchorRestoreService.cs (RelocatedToPlane decision)]
- **The device re-anchor + relocation render (actually MOVING the scene object to the relocated/re-acquired pose) is the AR rig scene (Story 6.3).** 3.5 decides WHERE the object should be (`TryRestore`'s `out Pose`); the actual re-parenting/re-anchoring of the scene `GameObject` to that pose is the AR rig scene wiring (the same not-yet-placed rig the 3.3/3.4 device glue waits on). Owner: Story 6.3. [Assets/Veilwalkers/AR/ArcoreAnchorProvider.cs (device path)]

## Deferred from: code review of 3-4-detect-planes-and-anchor-objects-with-occlusion-and-lighting (2026-06-14)

- **`OnCoachingChanged` has no initial-state replay for a late subscriber** — the event is change-only; a binder that subscribes (in `OnEnable`) AFTER a coaching message was already emitted gets no event until the next change, so it shows no banner despite the service being in `NeedsCoaching`. Triaged **out-of-scope for 3.4**: `OnCoachingChanged` is a documented "optional but recommended" convenience (NOT an AC — AC-2's "no spawn into empty space" is met by the `HasTrackablePlane` guard), and the AR-HUD coaching-banner binding is explicitly Epic 6 (the view + pixels are deferred). Epic 6 can either bind early enough to not miss the state, or request a `GetCurrentCoaching()` accessor from a small 3.4 follow-up. Owner: Epic 6 (the coaching-banner binder). [Assets/Veilwalkers/AR/PlaneAnchorService.cs (OnCoachingChanged)]
- **`FakeSpawnSink.Activate` is idempotent on `LiveIds`, so it cannot surface an illegal re-activate of a still-live id** — `Activate` does `LiveIds.Add(id)` unconditionally, so if `MonsterSpawner` ever Activated an already-active slot (a pooling bug), the fake's `LiveIds` would not catch it (only `ActivateCalls` moves). Triaged **out-of-scope for 3.4**: the story's anti-tautology bar asserts the SEAM CALL COUNTS (which `ActivateCalls` provides, non-idempotent), not full fake-internal-state verification, and the production spawner guards the Activate path with `!_slots[i].Active` so the bug cannot occur today. Test-infrastructure quality, not a defect. Owner: a future AR test-hardening pass (make `FakeSpawnSink.Activate` assert the id was deactivated first). [Assets/Tests/EditMode/AR.Tests/FakeSpawnSink.cs]
- **`MonsterSpawner.TrySpawn`'s "unreachable" error branch relies on `ActiveCount` staying in sync with the `Active` slot flags** — `ActiveCount` is a parallel counter (incremented in two branches, decremented in `Release`), not derived from the slots; a future code path that flips a slot's `Active` without adjusting `ActiveCount` would silently land in the "unreachable" refuse-and-log-Error branch. Triaged **out-of-scope for 3.4**: functionally correct today (every increment is immediately preceded by `Active = true`, every decrement by `Active = false`) and the branch itself is NFR-3-safe (refuses rather than crashing). A defensive invariant (derive `ActiveCount`, or a debug assert tying it to the count of `Active` slots) is a robustness enhancement, not a 3.4 AC. Owner: a future code-robustness / test-hardening pass. [Assets/Veilwalkers/AR/MonsterSpawner.cs (TrySpawn fall-through)]
- **`OnCoachingChanged` dedups on message TEXT, not OUTCOME state** — `PublishCoaching` gates on `string.Equals(_lastCoaching, message, Ordinal)`, so it cannot distinguish "still coaching, different reason" from "no change". Triaged **out-of-scope for 3.4**: there is exactly ONE coaching message today (`CoachingMessage`), so transitions are only `null ↔ CoachingMessage` and the value-based dedup is correct for all three paths (enter/leave/repeat). The latent issue only manifests if a future story adds a SECOND coaching message — that story owns switching the dedup to outcome-state-based. Owner: the story that introduces multiple coaching messages. [Assets/Veilwalkers/AR/PlaneAnchorService.cs (PublishCoaching)]
- **`MonsterSpawner.PooledCount` recomputes by iterating `_slots` on every read (no cached field)** — a theoretical concurrency risk: repeated reads in a tight loop could observe different states IF concurrent code mutated `_slots`. Triaged **out-of-scope for 3.4**: gameplay/AR is single-threaded and 3.4 builds no scene / owns no concurrent spawning; the property is consistent + correct within the single-threaded EditMode tests and at the real (once-per-interaction) call site. Owner: Story 6.3 (when the scene AR rig + concurrent interaction lands), if a hot-path or concurrent caller appears. [Assets/Veilwalkers/AR/MonsterSpawner.cs (PooledCount)]

## Deferred from: development of 3-4-detect-planes-and-anchor-objects-with-occlusion-and-lighting (2026-06-14)

- **`ArcorePlaneAnchorProvider` + `GameObjectSpawnSink` device/scene glue are logic-complete-but-subsystem-stub adapters (sanctioned scope, decision #2)** — The AR Foundation `ARPlaneManager`/`ARAnchorManager`/`ARRaycastManager` and the URP monster-prefab pool are scene-and-device-only + CI-untestable (architecture.md:591). The `#else` editor paths are real-enough that the placement/coaching/pooling LOGIC is reachable in-editor and fully proven against `FakeArPlaneAnchorProvider`/`FakeSpawnSink` (`ArcorePlaneAnchorProvider` reports NO plane so the editor exercises the AC-2 coaching path; `GameObjectSpawnSink` returns synthetic ids so the spawner bookkeeping runs); the `#if UNITY_ANDROID && !UNITY_EDITOR` device paths are `TODO(Story 6.3)` wired to the scene-placed AR rig when it lands. Owner: Story 6.3 (scene placement). [Assets/Veilwalkers/AR/ArcorePlaneAnchorProvider.cs; Assets/Veilwalkers/AR/GameObjectSpawnSink.cs]
- **AC-1 occlusion + environmental lighting are URP/AR-Foundation render features wired in the AR rig scene, NOT pure logic (deliberate altitude split)** — Story 3.4 lands the placement DECISION (`PlaneAnchorService` — rests-on-a-detected-plane / coaching-otherwise, pinned by `PlaneAnchorServiceTests`) which CI proves; the actual real-world-foreground occlusion (AR Foundation `AROcclusionManager` + the URP occlusion shader) and environmental lighting (`ARCameraManager` light-estimation → scene light) are render-pipeline + scene features that need the AR rig scene + a device camera/depth feed. They are device-visible, CI-untestable (architecture.md:591), and filed under "Rendering: URP; AR occlusion + environment lighting via AR Foundation" (architecture.md:229-230, :144). A literal AC-1-on-3.4-alone check reads "occlusion/lighting clause unmet" — that is inherent to the logic-vs-render split and lands in 6.3, NOT a defect. Owner: Story 6.3 (AR rig scene) / Epic 6 render polish. [docs/architecture.md:229-230, :144; Assets/Veilwalkers/AR/ArcorePlaneAnchorProvider.cs (device-path TODO)]
- **`ArPlacementView` + the placement/coaching trigger are logic-complete but unplaced + uncalled (the AR-HUD routing is Epic 6)** — The view exposes `RequestPlacement()` + resolves `PlaneAnchorService` via `GameServices.Get` degrading gracefully (inert → returns coaching) while unplaced — the 3.1/3.2/3.3 view precedent. The ACTUAL caller (the AR-HUD "tap/auto place" affordance), the coaching-banner pixels (the COPY is `PlaneAnchorService.CoachingMessage`; the styling/animation is Epic 6 UX), the `OnCoachingChanged` banner binder (which must unsubscribe symmetrically in OnDisable, architecture.md:514), and handing a `Placed` `AnchorToken` to the `EncounterService`/`EncounterSnapshot` are Epic 4/6. Wired into NO scene yet. Owner: Epic 6 (Story 6.3 placement + AR-HUD). [Assets/Veilwalkers/UI/ARHud/ArPlacementView.cs; Assets/Veilwalkers/AR/PlaneAnchorService.cs (OnCoachingChanged)]
- **The ≥30 FPS sustained device frame-rate budget (NFR-1) + SM-6 plane-anchor success rate are a device / Firebase-Test-Lab release-gate, not a unit test** — Story 3.4 lands the NFR-1 INSTRUMENTS that CI proves — `MonsterSpawner` pooling (reuse, not re-instantiate — pinned by `Spawn_release_spawn_reuses_...`) + the concurrent-spawn cap (refusal past N — pinned by `Spawns_up_to_the_cap_then_refuses_...`); the actual ≥30 FPS-on-mid-range measurement and the SM-6 plane-anchor-success telemetry are device measurements against percentiles (mirrors 3.3's "CI asserts the staging contract, device asserts the milliseconds"). The `MonsterSpawner` cap default (8) is a provisional tunable to be set by that pass / OQ balancing. Owner: AR-21 / a device-perf release-gate pass. [docs/architecture.md:229-230 (NFR-1), :597 (SM-6); Assets/Veilwalkers/AR/MonsterSpawner.cs]
- **[SETTLED in Story 3.5] The restore path (`TryRestoreAnchor` + frustum-nearest-plane relocation) is Story 3.5, and Story 3.5 ABSORBS 3.4's create-anchor seam (decision #6)** — Resolved 2026-06-14: `IArPlaneAnchorProvider` was renamed/absorbed into the single architecture-named `IArAnchorProvider` (forward members kept verbatim — `TryCreateAnchor` IS the "save" half, no separate `SaveAnchor`) with the restore members `TryReacquireAnchor` + `TryGetRelocationCandidates` (+ `PlaneCandidate`) ADDED; NO second create-anchor seam. `ArcorePlaneAnchorProvider`→`ArcoreAnchorProvider` and `FakeArPlaneAnchorProvider`→`FakeArAnchorProvider` renamed (GUIDs preserved via `git mv`); all 3.4 consumers updated; the 3.4 forward-path tests stayed green. The restore DECISION (`AnchorRestoreService.TryRestore`) + the frustum-nearest-plane relocation are built + tested. (Original note below.) Story 3.4 builds the FORWARD path (detect → place → create `AnchorToken` via `IArPlaneAnchorProvider.TryCreateAnchor`); the architecture-named `IArAnchorProvider.TryRestoreAnchor(token, out AnchorRestoreResult)` surface (architecture.md:409) + the `RelocatedToPlane` frustum-nearest-plane re-place (architecture.md:471-473) + the `AnchorToken.IsValid`/`None` validity contract (the 1.2 CR deferral owned by Story 3.5) are Story 3.5. **Reinvention guard:** the architecture names ONE anchor seam `IArAnchorProvider` owning BOTH save AND restore, and 3.5's AC-1 (epics.md:574-576) is the save/create half — the SAME native op as 3.4's `TryCreateAnchor`. So Story 3.5 must absorb `IArPlaneAnchorProvider` into `IArAnchorProvider` (rename + add restore, OR treat `TryCreateAnchor` as the save half + add restore), NOT define a second create-anchor surface. The `TryCreateAnchor` XML-doc carries this note. Owner: Story 3.5. [docs/architecture.md:409, :471-473; Assets/Veilwalkers/AR/IArPlaneAnchorProvider.cs (TryCreateAnchor note); Assets/Veilwalkers/Core/Contracts/AnchorToken.cs (IsValid — 3.5)]

## Deferred from: code review of 3-3-own-the-ar-session-lifecycle-with-prewarm (2026-06-13)

- **No test for `OnApplicationPause(true)` called from the `Prewarming` state** — the pause guard (`State != ArSessionState.Running` → warned no-op) correctly covers a pause-while-prewarming via its "otherwise" catch-all (logged warning, `_session.Pause()` not called, state unchanged), but no test exercises that specific illegal-transition path. Triaged **out-of-scope for 3.3**: the story's illegal-transition test list (Testing Requirements + the edge-case list) names `OnApplicationPause(true)` from `Cold`/`Ready` (not `Running`), not from `Prewarming`; the legal-transition table's "warned no-op otherwise" catch-all already specifies the behavior, and the code IS correct. This is test-completeness, not an AC gap or a defect. Owner: a future AR test-hardening pass (add a `Background_pause_from_prewarming_is_a_warned_no_op` case alongside the existing pause-from-`Cold` test). [Assets/Tests/EditMode/AR.Tests/ArSessionServiceTests.cs]

## Deferred from: development of 3-3-own-the-ar-session-lifecycle-with-prewarm (2026-06-13)

- **`ArcoreSession` device subsystem glue is a logic-complete-but-subsystem-stub adapter (sanctioned scope decision #5)** — The AR Foundation `ARSession` is a scene `MonoBehaviour` and Story 3.3 builds no scene (consistent with 3.1/3.2), and there is no `ARSession` in EditMode (architecture.md:591), so the device subsystem start/stop is genuinely device-only + CI-untestable. `ArcoreSession`'s `#else` editor path is REAL (reports `IsSupported`, no-op toggles) so the lifecycle is reachable in-editor and fully proven against `FakeArSession`; the `#if UNITY_ANDROID && !UNITY_EDITOR` device path is a documented `TODO(Story 3.4/6.3)` that wires to the scene-placed `ARSession` (enable/disable, `ARSession.state` for support, await tracking-ready) when the AR rig scene lands. The `ArSessionService` lifecycle STATE MACHINE (the real value of 3.3) is fully tested; only the untestable subsystem glue waits — exactly as 3.1/3.2 views are logic-complete-but-unplaced. **[OWNER RE-POINTED to Story 6.3 by Story 3.4, 2026-06-14]** — Story 3.4 built the plane/anchor/spawn LOGIC (`PlaneAnchorService`/`MonsterSpawner` + their seams + stub-and-defer adapters) but, like 3.1/3.2/3.3, builds NO scene; the AR Foundation `ARSession`/`ARPlaneManager`/`ARAnchorManager` are scene `MonoBehaviour`s, so the session subsystem AND the new plane/anchor/spawn subsystems all wire to the same not-yet-placed scene AR rig. All five `Story 3.4 / 6.3` references in `ArcoreSession.cs` (the block-comment TODO + the four `GameLog.Info` device-stub strings) were updated to `Story 6.3`. Owner: Story 6.3 (scene placement). [Assets/Veilwalkers/AR/ArcoreSession.cs (device-path `#if` stub)]
- **[SETTLED in Story 4.1] `OnArSessionInterrupted` is declared + raised, but the `EncounterStateMachine` `Suspended` consumer + `TryRestoreAnchor` recovery are downstream** — Story 3.3 raises the event; Story 3.5 built the `TryRestoreAnchor` recovery DECISION; **Story 4.1 (2026-06-14) built the CONSUMER + the recovery CALL SITE** — `EncounterService` subscribes to `OnArSessionInterrupted` → `EncounterStateMachine.Suspended` + guidance, and `Recover()` invokes `AnchorRestoreService.TryRestore` to re-anchor (Restored/RelocatedToPlane resume; Failed stays Suspended). The full three-story seam (3.3 raises → 3.5 decides → 4.1 consumes) is now closed at the LOGIC level; only the scene GameObject re-anchor render (Story 6.3) + the Lure-VFX beat (Epic 4/6) remain. Pinned by `EncounterServiceTests`. Owner of the remaining render: Story 6.3 / Epic 6. [Assets/Veilwalkers/AR/ArSessionService.cs (event); Assets/Veilwalkers/AR/AnchorRestoreService.cs (the decision); Assets/Veilwalkers/Encounter/EncounterService.cs (the consumer + caller); architecture.md:650-652]
- **`ArSessionView` + the prewarm/enter/back-out triggers are logic-complete but unplaced + uncalled (the AR-entry routing is Epic 6)** — The view exposes `Prewarm()`/`EnterAr()`/`BackOut()` (fault-observed fire-and-forget) + pumps `OnApplicationPause`, and resolves `ArSessionService` via `GameServices.Get` degrading gracefully (inert + `GameLog.Warn`) while unplaced — the 3.1/3.2 view precedent. But the ACTUAL callers — Onboarding/Home gating `Prewarm()` to "AR-entry affordance visible/likely" (architecture.md:588-589), and the "Enter AR Hunt" affordance binding `EnterAr()` — are Epic 6 (Story 6.3 AppStateMachine / 6.4 onboarding). The Epic-6 "veil-parting wipe" ritual prewarm cover (architecture.md:609-612) also defers. Wired into NO scene yet. Owner: Epic 6 (Story 6.3/6.4). [Assets/Veilwalkers/UI/ARHud/ArSessionView.cs; Assets/Veilwalkers/App/LoadPhase.cs (the staging contract — Bootstrap stages the shape; the prewarm trigger is Onboarding/Home's gated call)]
- **The launch→first-anchor millisecond budget + first-Monster preload are a device / Firebase-Test-Lab release-gate, not a unit test (NFR-5)** — Story 3.3 lands the staging CONTRACT (`LoadPhase { EssentialSync, WarmupAsync, Ready }` + "warmup not awaited before Ready / construction does not warm", AC-CS-2) which CI asserts (`LoadPhaseContractTests` + `ArSessionServiceTests.Construction_does_NOT_warm_...`). The actual cold-start millisecond budget, the "measure with prewarm on AND off; drop it if it shaves little" decision (architecture.md:588-590), AND the first-Monster preload call site (there is no `MonsterDatabase.asset` to preload yet — the open Story 2.2 `[~]` deferral) are all downstream. Owner: AR-21 / a device-perf release-gate pass (ms budget); the Editor-authoring session that closes the 2.2 `.asset` + Epic 6 Home/Onboarding (the preload call site). [docs/architecture.md:577-596; Assets/Veilwalkers/App/LoadPhase.cs (WarmupAsync — preload call site is downstream)]

## Deferred from: code review of 3-2-gate-ar-entry-behind-the-mandatory-safety-warning (2026-06-13)

- **`ArSafetyView.Render` switch has no `default` case** — the switch covers all four current `ArSafetyGateState` values (`Inactive`/`BlockingFull`/`BlockingFastCard`/`Acknowledged`) and is therefore exhaustive today, but if `ArSafetyGateState` ever gains a value, `Render` would silently ignore it (no warn, no draw). Triaged **out-of-scope for 3.2**: `Render` is an explicit Epic-6 stub (`GameLog.Info` breakdown with a `TODO(Epic 6)` at the bind site), so defensive exhaustiveness belongs to the Epic-6 rewrite that binds the real chunky warning UI — the identical disposition as Story 3.1's `CameraPermissionView.Render` no-default finding. Note: unlike `ArEntryKind` (whose XML-doc pins its members as final — "only ColdEntry/ShopResume exist now"), `ArSafetyGateState` is NOT declared immutable, so a future state-adding story would also own adding the default at the point it adds the state. Owner: Epic 6 Story 6.3 (the real `Render` binding) or whatever story extends `ArSafetyGateState` — add `default: GameLog.Error(...)` (or a compiler-enforced exhaustiveness pragma) then. [Assets/Veilwalkers/UI/ARHud/ArSafetyView.cs (Render switch)]

## Deferred from: development of 3-2-gate-ar-entry-behind-the-mandatory-safety-warning (2026-06-13)

- **`ArSafetyGate.MayInteract` is the AC-1 block predicate, but no surface READS it yet (deliberate altitude split, not an unmet AC)** — Story 3.2 built + proved the gate's `MayInteract` (the single AC-1 predicate: camera view + Lure/Encounter actions are reachable ONLY when the warning is `Acknowledged`; the fast card is still blocking, so it cannot be A/B'd away). What is deferred is the CONSUMER side: the camera view, the Lure affordance, and the Encounter-entry that must CHECK `MayInteract` before allowing any action. Until those land, the predicate is logic-complete (fully covered by `AR.Tests`) but has no reader, so AC-1's "camera view not interactive / no Encounter action reachable until acknowledged" is enforceable-by-construction but not yet enforced in any scene. Deferred reason: 3.2's altitude is the gate decision + its predicate (the [[ui-presenter-pattern]] split); the surfaces that obey it are Epic 4 (EncounterService entry gate) / Epic 6 (AR-HUD), placed + routed (with the `ArEntryKind` supplied from AR-entry navigation) by Story 6.3. `ArSafetyView` is likewise logic-complete but runtime-INERT until placed — it resolves `ArSafetyGate` via `GameServices.Get` and degrades gracefully (no-op + warn) while unplaced, mirroring the 3.1 `CameraPermissionView` deferral. Owner: Epic 4 (Encounter entry reads `MayInteract`) + Epic 6 (AR-HUD + scene placement, Story 6.3). [Assets/Veilwalkers/AR/ArSafetyGate.cs (MayInteract); Assets/Veilwalkers/UI/ARHud/ArSafetyView.cs (unplaced/inert)]
- **Session-scoped warning state only — no persisted "seen-before" (sanctioned scope decision #4)** — the "shown the full read this session" latch (`ArSafetyGate._shownThisSession`) is in-memory only; a fresh app launch re-shows the full deliberate read, and later cold entries within the same app session show the fast card (AC-2). A persisted "has the player EVER read the full warning" (so a returning player gets the fast card on their very first cold entry of a NEW session) is deliberately NOT built: (a) the AC's "first per session" most naturally means per app session, and (b) erring toward showing the full read MORE often is the safe direction for a mandatory Google-Play safety gate (showing it more is never a compliance risk; showing it less could be). Deferred reason: no AC requires persistence, and a `SaveModel` flag + a `SaveService`/`IClock` dependency would be added scope with a compliance-risk downside. Owner: Epic 6 onboarding / an OQ balancing decision, IF a returning-player fast-card-on-first-cold-entry is ever wanted — add a `SaveModel.SafetyWarningSeen` bool + inject `SaveService` into the gate then. [Assets/Veilwalkers/AR/ArSafetyGate.cs (in-memory `_shownThisSession` latch)]

## Deferred from: code review of 3-1-disclose-and-request-camera-permission-gracefully (2026-06-15)

- **[SETTLED in Story 3.3] `Granted` camera-permission state is never re-validated, so a permission revoked while the app is backgrounded is missed** — Resolved 2026-06-13: `ArSessionService` (the AR-lifecycle owner, Story 3.3) re-validates camera permission via the injected `CameraPermissionFlow.Evaluate()` (live OS truth) at TWO points: (a) before `IArSession.StartAsync` in `PrewarmAsync`/`EnterAsync` (`CanStart()` refuses + stays `Cold` if not `Granted`), and (b) on backgrounded-resume in `OnApplicationPause(false)` — a permission revoked while `Paused` does NOT silently `Resume()`; it tears the session down to `Cold` and raises `OnArSessionInterrupted` so 3.1's `CameraPermissionView` re-grant flow takes over. Pinned by `Permission_revoked_while_backgrounded_does_NOT_resume_tears_down_and_interrupts` (asserts `Resume()` NOT called + the event fires once) and `Ungranted_camera_permission_refuses_to_prewarm_and_stays_cold`. The re-grant UX itself remains 3.1's already-built `CameraPermissionView`; 3.3 only refuses + routes. (Original note: `CameraPermissionView.OnApplicationFocus` only re-queried while `Requesting`/`Denied`, so a revocation while backgrounded would let AR proceed on a stale `Granted` — the place to re-check is AR entry, owned by Story 3.3.) [Assets/Veilwalkers/AR/ArSessionService.cs (CanStart + OnApplicationPause(false)); Assets/Veilwalkers/AR/CameraPermissionFlow.cs (Evaluate, the re-check seam)]

## Deferred from: code review of 2-5-view-a-monsters-detail-entry (2026-06-14)

- **DisplayName placeholder invariant is not applied on the discovered-but-unauthored path** — `CodexDetailPresenter.BuildDetail` null-coalesces a null *authored* name to `UnnamedPlaceholder`, but a discovered-but-unauthored id (no `MonsterDefinition`) gets `DisplayName = null` (no definition to placeholder from). So the two "no name" cases render inconsistently: an authored-null name shows "(unnamed)", an unauthored discovered id shows a blank name. Harmless in 2.5 because `CodexDetailView.Render` is an explicit Epic-6 stub (a `GameLog.Info`), but when Epic 6 binds real widgets the binding code should normalize this (apply the placeholder for ANY null `DisplayName`, or have the view treat null name uniformly) so the panel never shows a blank title. Deferred reason: 2.5's altitude is the presenter + view-model logic; real widget binding/layout is Epic 6 (UX-DR5 / Story 6.2). Owner: Epic 6 detail-panel binding. [Assets/Veilwalkers/UI/Codex/CodexDetailPresenter.cs (def==null branch); Assets/Veilwalkers/UI/Codex/CodexDetailView.cs (Render stub)]
- **`CaptureDifficulty`/`SlayDifficulty` overload 0 as both a legitimate authored value and the not-discovered/unauthored sentinel** — `CodexDetailViewModel` carries the stats as plain ints; `ForNotDiscovered` and the unauthored-discovered path both pass `0`, but an authored definition with a real `CaptureDifficulty == 0` is indistinguishable from "no stat" at the view layer. A consumer cannot tell "difficulty 0" from "not leaked". Safe in 2.5 because the AC-2 anti-leak rule requires a consumer to gate on `IsDiscovered` before reading any content (stats included), and stats are PROVISIONAL (OQ-9) and only displayed here. Deferred reason: stat *semantics*/validation (incl. whether 0 is meaningful, and the still-open negative/overflow-bound deferrals from 2.1) are Epic 4 / the OQ-9 balancing pass; 2.5 only renders. Owner: Epic 4 / OQ-9 — the Epic-6 binder must check `IsDiscovered` first, and balancing decides whether 0 is a valid authored stat. [Assets/Veilwalkers/UI/Codex/CodexDetailViewModel.cs]

## Deferred from: development of 2-5-view-a-monsters-detail-entry (2026-06-14)

- **Codex detail-panel visual render deferred to Epic 6 (deliberate altitude split, not an unmet AC)** — Story 2.5 built the *decision* behind the detail panel (discovered → name/tier/stats/lore/first-discovered date; undiscovered → the "Not yet discovered." copy with the AC-2 anti-leak guarantee; the null-safe content) as a headless-tested `CodexDetailPresenter` → Unity-free `CodexDetailViewModel`, plus a thin logic-free `CodexDetailView` that binds it. The *pixels* are explicitly deferred: real monster art draw, the rarity-tier **badge color** (UX-DR2 tokens / Story 6.1 — the view-model carries `Tier`, Epic 6 maps it), the panel layout/typography/chunky styling (UX-DR5 / Story 6.2), and the open/close transition. `CodexDetailView.Render` is a stubbed `GameLog.Info` with a `TODO(Epic 6)` at the bind site. Owner: Epic 6 (Stories 6.1/6.2), with the grid→detail tap routing + scene placement by Story 6.3. [Assets/Veilwalkers/UI/Codex/CodexDetailView.cs; Assets/Veilwalkers/UI/Codex/CodexDetailViewModel.cs]
- **`CodexDetailView` is logic-complete but runtime-INERT until CodexService is registered** — same blocker as `CodexGridView` (2.4): the view resolves `CodexService` via `GameServices.Get` and degrades gracefully (inert `Open(id)` no-ops + `GameLog.Warn`) because CodexService is still unregistered in Bootstrap (blocked by the unauthored 2.2 `MonsterDatabase.asset`). The detail logic is fully proven headless (`Veilwalkers.UI.Tests`), independent of wiring; the panel lights up once the `.asset` is authored and Bootstrap registers CodexService. Wired into NO scene yet — Story 6.3 places it + routes the grid tap to `Open(slot.Id)`. Owner: the Editor-authoring session that closes the 2.2 `.asset` deferral + Bootstrap wiring, then Story 6.3. [Assets/Veilwalkers/App/Bootstrap.cs (seam); Assets/Veilwalkers/UI/Codex/CodexDetailView.cs]

## Deferred from: code review of 2-4-browse-the-67-grid-with-hybrid-gradual-reveal (2026-06-13)

- **`HighestDiscoveredRarity` does a `MonsterDatabase.TryGet` per discovered id on every call, uncached across the service** — the 2.4 CR patch eliminated the *grid presenter's* per-slot re-scan (the high-water mark is now computed ONCE per `BuildSlots` via `HighestDiscoveredRarity()` and passed to the `IsTierBegun(tier, floor)` overload), so the presenter's per-rebuild cost dropped from O(slots × discovered) to O(discovered). What remains deferred is the service-layer micro-cost: each `HighestDiscoveredRarity()` call still does one dictionary `TryGet` per discovered id, with no memoization between calls. At the real call site (once per rebuild, on discovery) this is correct and cheap; caching it would be dead complexity today and could go stale as the codex grows. Deferred reason: it's a latent service-layer design choice, not a 2.4 AC, and any future consumer (Epic 4 Encounter composing Capture/Slay reveal) that calls it in a hot path should drive the cache decision. Owner: a future performance/Encounter story that establishes a hot-path caller. [Assets/Veilwalkers/Monsters/Codex/CodexService.cs]

## Deferred from: development of 2-4-browse-the-67-grid-with-hybrid-gradual-reveal (2026-06-13)

- **Codex grid visual render deferred to Epic 6 (deliberate altitude split, not an unmet AC)** — Story 2.4 built the *decision* behind every Codex slot (which of Discovered / `???` Silhouette / `?` Blank each of the 67 slots is in, the Dread-Ladder begun-tier computation, the X/67 count, and the recompute-on-discovery that drives the flip + count-tick) as a headless-tested `CodexGridPresenter`, plus a thin logic-free `CodexGridView` that binds it. The *pixels* are explicitly deferred: real monster art, the Pumpkin-Patch tier-color tokens (UX-DR2 / Story 6.1), the tier-tinted "caught" stamp, the silhouette→art *flip* animation and the count-*tick* animation (UX-DR15 / Story 6.5), and the chunky 3-state Codex-slot **component** (UX-DR5 / Story 6.2). The presenter exposes everything those need (per-slot `State` + `Tier` + the before/after counts), so they become pure rendering when the component lands. `CodexGridView.Render` is a stubbed `GameLog.Info` breakdown with a `TODO(Epic 6)` at the bind site. Owner: Epic 6 (Stories 6.1/6.2/6.5), with placement into a scene by Story 6.3. [Assets/Veilwalkers/UI/Codex/CodexGridView.cs; Assets/Veilwalkers/UI/Codex/CodexGridPresenter.cs]
- **`CodexGridView` is logic-complete but runtime-INERT until CodexService is registered** — the view resolves `CodexService` via `GameServices.Get` and degrades gracefully (inert grid + `GameLog.Warn`) because CodexService is still unregistered in Bootstrap (the 2.3 seam, blocked by the unauthored 2.2 `MonsterDatabase.asset`). The grid's reveal logic is fully proven headless (`Veilwalkers.UI.Tests`), independent of wiring; the production grid lights up once the `.asset` is authored and Bootstrap registers CodexService (the same `MonsterDatabase.asset` blocker as the 2.2/2.3 deferrals below). The view is also wired into NO scene yet — Story 6.3 places it. Owner: the Editor-authoring session that closes the 2.2 `.asset` deferral + Bootstrap wiring, then Story 6.3 for scene placement. [Assets/Veilwalkers/App/Bootstrap.cs (seam); Assets/Veilwalkers/UI/Codex/CodexGridView.cs]

## Deferred from: code review of 2-3-read-and-persist-codex-collection-state (2026-06-13)

- **`FakeCodexProgressStore.SaveAsync` reads/resets `FailNextSave` and reads `SaveGate` non-atomically across the await** — `SaveCalls` uses `Interlocked`, but the `FailNextSave` check-then-reset and the `SaveGate` read are plain field ops, so two concurrent `SaveAsync` calls (the `SaveGate` affordance exists to hold one save mid-flight while another proceeds) could race the fail path. Test-only and not exercised by the current suite (no test fires concurrent `SaveAsync` + `FailNextSave`); gameplay is single-threaded (the 2.2 deferral rationale). Deferred reason: the story explicitly defers concurrency (Dev Notes: "Gameplay is single-threaded") and mandates no thread-safety on the fake's fail/gate fields; this is a latent test-harness quality issue, not a 2.3 AC. Owner: a future concurrency-hardening story, or whenever a test actually drives concurrent saves through the fail path — make `FailNextSave`/`SaveGate` access atomic (or document the fake as single-save-at-a-time). [Assets/Tests/EditMode/Monsters.Tests/FakeCodexProgressStore.cs]
- **[SETTLED in Story 2.4] `CodexService.DiscoveredIds` allocates a fresh `List<string>` on every property read** — Resolved 2026-06-13: reshaped the `DiscoveredIds` property → `GetDiscoveredIds()` method, so the method shape signals "this allocates / is a snapshot — call once." The actual consumer (`CodexGridPresenter.BuildSlots`) calls it ONCE per rebuild into a `HashSet`, so a single allocation per rebuild is correct; no cache/invalidation was added (it would be dead complexity at the real access pattern). Pure rename — 2.3 had shipped `DiscoveredIds` with no production consumer. (Original note: a per-frame UI binding would churn allocations, and the property shape understated the cost — the method shape + once-per-rebuild access is the honest fix.) [Assets/Veilwalkers/Monsters/Codex/CodexService.cs]
- **Recovery-swap branch of `RecordDiscoveryAsync` is not directly tested** — the persist-failure (exception) path IS tested via `FakeCodexProgressStore.FailNextSave`, but the swap branch (`!ReferenceEquals(_saveService.Current, model)` — recovery replaces `Current` mid-persist) is not exercised; it needs the `SaveGate` + concurrent-recovery harness. The CR DID harden this branch's returned count to report `_saveService.Current?.Codex.Count` (the live model), mirroring the `DailyRewardService` swap-branch contract. Deferred reason: a recovery racing a codex discovery is heavier than 2.3's unit-level scope; the swap mechanism itself is proven by the existing Economy swap tests on the same shape. (Mirrors the identical 1.8 deferral.) Owner: a future concurrency-test pass, or Epic 4 (which composes the Capture/Slay multi-delta write under recovery). [Assets/Tests/EditMode/Monsters.Tests/CodexServiceTests.cs]

## Deferred from: code review of 2-2-register-the-67-monster-database-with-mvp-content (2026-06-13)

- **`MonsterDatabase` lazy ID index is not thread-safe** — `EnsureIndex`/`TryGet` build `_byId` lazily with no lock; two concurrent first-callers could each build and the second overwrite the first. Deferred reason: Unity gameplay is single-threaded and `MonsterDatabase` is a main-thread read-only static registry; the architecture is premised on single-threaded execution (only async I/O — SaveService/Billing/ArSession — runs off-thread, never catalog access). Owner: a future backend-sync (OQ-3 Firebase) or background-threading story, if one arises — add a `lock`/`Lazy<T>` then. [Assets/Veilwalkers/Monsters/MonsterDatabase.cs]
- **AC-4 on-disk asset-filename audit + populated-count `[3..5]` real-asset check are not performed** — these require the 3–5 MVP `.asset` files + the `MonsterDatabase.asset`, which Story 2.2 Task 3 deferred `[~]` to an Editor-authoring session (a `Sprite` `Art` ref needs an imported texture; a null `Art` fails `Validate()`, so real content needs the Editor). The registry + validation + `Count==67` are fully done and tested via `SetForTests`; only the on-disk production audit waits. Until the assets exist, Story 2.1's tautological `Asset_file_name_convention_is_Id_underscore_Name` test remains in place (its falsifiable disk-scan replacement can't be written without assets — sequencing guard). Owner: an Editor-authoring session completing Story 2.2 Task 3 — author the MVP monsters (incl. ≥1 Rare+ with real art), assign them into `MonsterDatabase.asset`, then add the on-disk audit (load via `AssetDatabase`, assert `Validate()` empty + filenames match `<Id>_<Name>` + `PopulatedCount ∈ [3..5]`) and delete the old tautological test. [Assets/Veilwalkers/ScriptableObjects/ (assets); Assets/Tests/EditMode/Monsters.Tests/MonsterDefinitionTests.cs (old test); Assets/Tests/EditMode/Architecture.Tests/MonsterDatabaseLoreCountTests.cs (on-disk audit)]

## Deferred from: code review of 2-1-define-monster-data-and-the-ordered-rarity-model (2026-06-13)

- **AC-4 asset-name test is a tautology (no production code derives the name yet)** — `Asset_file_name_convention_is_Id_underscore_Name` builds `<Id>_<Name>` by hand and asserts it equals the literal computed from the same inputs; it exercises no production path, so a real misnamed `.asset` on disk would not be caught. Deferred reason: Story 2.1 explicitly excludes the `MonsterDatabase` registry and authoring `.asset` files (Dev Notes "IS NOT"); a falsifiable AC-4 check needs a production name-deriver or a disk scan, which arrives with the registry. Owner: Story 2.2 (registry/asset authoring) — replace with a check against the production naming helper or an actual-filename scan. [Assets/Tests/EditMode/Monsters.Tests/MonsterDefinitionTests.cs]
- **[SETTLED in Story 2.5 — render layer] `MonsterDefinition.DisplayName` accepts null with no guard** — Resolved 2026-06-14: the first real render consumer is the Codex detail view (Story 2.5), and `CodexDetailPresenter.BuildDetail` now null-coalesces a null authored name to `CodexDetailPresenter.UnnamedPlaceholder` ("(unnamed)") — it NEVER throws on a null name (pinned by `Discovered_authored_slot_with_null_name_renders_placeholder_not_a_crash`). The TYPE still accepts null at `SetForTests`/serialization (unchanged — 2.1's shape decision stands, and authored-content validation that non-null at the asset layer remains Story 2.2's `MonsterDatabase` content concern); what's settled is the render-layer crash risk this deferral named ("`NullReferenceException` in downstream render calls, Codex detail"). The original note mis-attributed the owner to "Story 2.3 (UI null-safety)" but 2.3 was data-only — 2.5 is the actual UI consumer. [Assets/Veilwalkers/UI/Codex/CodexDetailPresenter.cs]
- **[SETTLED in Story 2.5 — render layer] `Lore` accepts null with no guard** — Resolved 2026-06-14: `CodexDetailPresenter.BuildDetail` null-coalesces a null lore to `string.Empty` — no Codex-detail string op crashes (pinned by `Discovered_authored_slot_with_null_lore_renders_empty_not_a_crash`). Same scope as DisplayName: the render-layer crash is settled; the type-shape/asset-validation remains 2.2's. [Assets/Veilwalkers/UI/Codex/CodexDetailPresenter.cs]
- **[SETTLED in Story 2.5 — render layer] `Art` Sprite can be null on an authored asset** — Resolved 2026-06-14 at the render decision: the detail view-model is Unity-free (decision #2) and carries NO `Sprite` — it never dereferences `art.name`/draws art; the Epic-6 binder resolves `Art` from the id at bind time and is responsible for a null-art fallback (UX-DR5, recorded in the 2.5 Epic-6 render deferral). So the "UI assumes non-null when rendering" crash this deferral named cannot occur in 2.5's render model. Authored assets requiring non-null art at the content layer remains Story 2.2's concern. Owner of the Epic-6 fallback draw: Epic 6 (Story 6.2). [Assets/Veilwalkers/UI/Codex/CodexDetailViewModel.cs (carries no Sprite); Epic 6 for the fallback draw]
- **`CaptureDifficulty`/`SlayDifficulty` accept negative values, no lower bound** — a `-5` passes silently; downstream Encounter math (Story 4) could invert success ratios or divide oddly. Deferred reason: 2.1 locks the static-data *shape*, not balancing; the stats are flagged PROVISIONAL — OQ-9, and AC-1 only requires that base stats exist as fields. Owner: Story 4 (Encounter, where consumed) or the OQ-9 balancing pass — add range validation where the values are read/authored. [Assets/Veilwalkers/Monsters/MonsterDefinition.cs]
- **`CaptureDifficulty`/`SlayDifficulty` have no upper bound (overflow risk downstream)** — `int.MaxValue` passes; Encounter bonus math could wrap. Same deferral rationale as the negative-bound item (shape vs. balancing). Owner: Story 4 / OQ-9. [Assets/Veilwalkers/Monsters/MonsterDefinition.cs]
- **Deserialized out-of-range `Rarity` (e.g. `(Rarity)99`) loads silently** — a corrupt or future-version `.asset` with an out-of-`[0,4]` rarity int deserializes without complaint; `>=` floor comparisons still *work* but the value is out-of-spec. Deferred reason: 2.1 is the enum/type shape (correctly ordered + value-stable); validating deserialized asset values is asset-load integrity, owned by the registry. Owner: Story 2.2 `MonsterDatabase` load/validation (reject or clamp-and-log an out-of-range tier). [Assets/Veilwalkers/Monsters/Rarity.cs]

## Deferred from: code review of 1-8-claim-a-daily-free-credit-reward (2026-06-13)

- **No overflow-boundary test for the daily claim (`int.MaxValue − small + reward`)** — `DailyRewardService.TryClaimAsync` wraps the credit add in a `checked` block; the CR moved that block inside the inner try so an `OverflowException` now converts to a typed `PersistenceFailed` result (with both fields rolled back) rather than escaping. But no test drives a balance near `int.MaxValue` to exercise that conversion. Deferred reason: Story 1.8's Task 5 test list does not include an overflow boundary, and whether to replicate `CreditService`'s `int.MaxValue` boundary test across every Economy mutator is a broader test-maturity decision, not a 1.8 AC. Owner: a future Economy test-hardening pass (or whenever overflow handling is centralized). [Assets/Tests/EditMode/Economy.Tests/DailyRewardServiceTests.cs]
- **No year-boundary calendar test (Dec 31 → Jan 1) for the day-rollover** — the rollover test crosses a same-month boundary (06-13 → 06-14). The `yyyy-MM-dd` lexicographic comparison is correct across year boundaries (string order == date order), so this is defensive redundancy, not a defect. Deferred reason: AC-3's decision-risk tests (UTC-vs-local, day-granular-not-instant, and now future-date rejection) are present; exhaustive Gregorian-boundary coverage (year/leap-day) is test-suite maturity, not a 1.8 requirement. Owner: a future calendar-edge test pass. [Assets/Tests/EditMode/Economy.Tests/DailyRewardServiceTests.cs]
- **Event-subscriber reentrancy into `TryClaimAsync` not tested** — `OnDailyRewardClaimed` is raised outside the mutation lock, so a subscriber could re-enter `TryClaimAsync` (it would be correctly refused by the same-day guard). Deferred reason: the service's contract (correct same-day refusal regardless of caller) is fulfilled and the event-outside-lock semantics are documented; what UI subscribers do with the event — including reentrancy — is Epic 6 (daily-reward UI). Owner: Epic 6. [Assets/Veilwalkers/Economy/DailyRewardService.cs]
- **Recovery-swap path (`ReferenceEquals(_saveService.Current, model)` mismatch) not directly tested** — the persist-failure (exception) path IS tested via `FakeProgressStore.FailNextSave`, but the swap branch (recovery replaces `Current` mid-persist) is not exercised; it needs the `SaveGate` + concurrent-recovery harness the `ChargePipelineTests` use. The CR did harden this branch's returned balance (now reports `_saveService.Current?.Credits`). Deferred reason: a recovery racing a daily claim is heavier than 1.8's unit-level claim-logic scope; the swap mechanism itself is proven by the existing `CreditService`/`ProgressionService` swap tests on the same shared lock. Owner: a future Economy concurrency-test pass (or Epic 4/6, which compose multi-step actions under recovery). [Assets/Tests/EditMode/Economy.Tests/DailyRewardServiceTests.cs]

## Deferred from: code review of 1-7-grant-20-starting-credits-on-first-launch (2026-06-13)

- **No re-entrancy / in-flight guard on `FirstLaunchGrant.RunAsync`** — Two concurrent `RunAsync` calls could both observe `StartingCreditsGranted == false` before either persists the marker and double-grant. Not reachable today: the sole caller is Bootstrap's post-load continuation, which runs exactly once per boot; the marker write is now serialized under the shared `SaveMutationLock`, but the read-decide-grant span as a whole is not guarded against a second concurrent caller. Deferred reason: there is no second caller, and adding an in-flight guard (or making the grant a one-shot) is best designed alongside AR-14 awaited boot staging (Epic 3/6) when call ordering is formalized — premature here. Owner: AR-14 / whenever a second caller appears. [Assets/Veilwalkers/App/FirstLaunchGrant.cs]

## Deferred from: code review of 1-6-configure-economy-values-data-drivenly (2026-06-13)

- **No overflow / upper-bound check on `EconomyConfig._levelXpThresholds` at asset-edit time** — A designer can set a threshold near `int.MaxValue`; `BuildProgressionRules` passes it to the `ProgressionRules` ctor, which validates strictly-increasing but not magnitude. The overflow then surfaces at runtime in `ProgressionService.AddXpAsync`'s `checked` arithmetic (thrown + logged, never silent) — the documented contract. Deferred reason: `BuildProgressionRules` deliberately does not re-validate (story deviation #4, single validation source = the ctor), and 1.6's scope is the data-driven plumbing, not balancing oversight. Owner: a future OQ-9 balancing / economy-oversight story (could add an asset-edit-time range warning to `OnValidate`). [Assets/Veilwalkers/Economy/EconomyConfig.cs]
- **No runtime-immutability enforcement on `EconomyConfig._levelXpThresholds`** — The class doc says "static data only … nothing here is mutated at runtime," but the `int[]` field has no `Array.AsReadOnly`/`ImmutableList` guard against reflection or an accidental future mutator. Risk is low: `ProgressionRules`' ctor makes a defensive copy, so a post-build source mutation cannot reach the injected rules. Deferred reason: no AC requires a runtime immutability guard; "static data only" is a design principle enforced by the defensive copy at the ctor boundary, and the test seam (`internal SetForTests`) is the sanctioned test-only mutation path. Owner: a future immutability-hardening pass (wrap the field, or expose it read-only). [Assets/Veilwalkers/Economy/EconomyConfig.cs]

## Deferred from: code review of 1-5-earn-xp-and-award-charges-via-progression (2026-06-12)

- **No cross-service serialization test for `AddXpAsync` under the shared `SaveMutationLock`** — The shared-lock serialization test (`ChargePipelineTests`) pairs a `CreditService` spend with a charge consume, proving the lock interleaves two Economy mutators. But `AddXpAsync` — the widest mutation in the codebase (XP + level + three charge counts) and therefore the highest-impact serialization regression target — is not pinned racing another mutator under the lock. Deferred reason: the lock itself is proven by the existing test; this is additive coverage of the same mechanism, not a defect, and the patch findings (rollback-window hardening) are the blocking items for this review. Owner: fold into the next Economy test-hardening pass (or Epic 4, which composes multi-delta actions and will exercise `AddXpAsync` under concurrency). [Assets/Tests/EditMode/Economy.Tests/ChargePipelineTests.cs]

## Deferred from: code review of 1-4-grant-and-spend-credits-through-the-ledger (2026-06-12)

- **No timeout/cancellation on the credit mutation lock or persist await** — A store write that hangs (e.g. Android scoped-storage stall) holds `CreditService`'s `SemaphoreSlim` forever; every subsequent spend/grant queues behind it for the session with no typed failure ever returned, and `ICreditService` has no `CancellationToken` plumbing to fix it from outside. Deferred reason: cancellation/timeout design belongs with the AR-19 long-op escalation surface (Epic 6 status UI / later hardening), and adding tokens piecemeal to one service would fragment the contract. Owner: Epic 6 / post-MVP hardening. [Assets/Veilwalkers/Economy/CreditService.cs:62,126]
- **Negative `SaveModel.Credits` loaded from disk passes every ledger guard silently** — The ledger validates its inputs (`cost`/`amount > 0`) but never its invariant (`Credits >= 0`): a negative balance arriving via a migration bug, reconciliation replay (the 5.2 scenario the grant's `checked` comment names), or tampered-but-decryptable save propagates unflagged into `Balance`, UI, and future saves. Deferred reason: load-time invariant validation belongs to Persistence/migration, not per-read checks in Economy. Owner: Story 5.2 (reconciliation) + Story 1.9 (telemetry counter for the anomaly); consider a clamp-and-log at `SaveService` load. [Assets/Veilwalkers/Economy/CreditService.cs:46,70]

## Deferred from: code review of story-1.1 (2026-06-11)

- **`.aab` Build-App-Bundle toggle not committed** — The "Build App Bundle (Google Play)" output mode is stored in `Library/EditorUserBuildSettings.asset`, which is correctly gitignored. A fresh `git clone` + Unity open will NOT carry the .aab output setting, so a build machine / CI / teammate must re-toggle it before building, or could emit an APK instead of the AAB Play requires. Sanctioned as a release-gate item (story Task 6 marked `[~]`; Testing Requirements pre-authorize the full device build as a release-gate, not a unit gate). Owner: AR-21 (build/deploy). Add a one-line build-setup note there so it isn't a surprise.
- **`AndroidTargetSdkVersion: 0` (Automatic = highest installed SDK)** — Non-reproducible: two build machines can bake different `targetSdkVersion` into the build, and Google Play enforces a target-SDK floor that ratchets up over time. Deferred reason: target SDK is a release-gate decision (Play floor moves over time); Story 1.1 is scoped to foundation-opens-and-compiles. Owner: AR-21 — pin an explicit target SDK there alongside the .aab output mode + real bundle ID so all three non-reproducible build settings are fixed together. [ProjectSettings/ProjectSettings.asset:177]
- **`applicationIdentifier` empty / package name still template default `com.UnityTechnologies.*`** — Cannot ship to Google Play with the template applicationId. Not an acceptance criterion of Story 1.1; already flagged in the story's Dev Agent Record as a non-blocking follow-up. Must be set to a real bundle ID (e.g. `com.veilwalkers.app`) before any Play upload. Owner: AR-21 (build/deploy).

## Deferred from: story-1.2 (2026-06-12)

- **EDM4U Android Gradle templates not committed (only the folder is)** — The Google External Dependency Manager (EDM4U), bundled with ARCore Extensions, auto-resolves Android dependencies and copies `mainTemplate.gradle` into `Assets/Plugins/Android/`. That folder is now committed as a tracked-but-empty stub (`.gitkeep` + `.meta`) so the resolver has a copy target — without it, every Android auto-resolve throws `DirectoryNotFoundException: …/Assets/Plugins/Android/mainTemplate.gradle` (first hit after Story 1.2). The EDM-**generated** templates themselves (`mainTemplate.gradle`, `gradleTemplate.properties`, `settingsTemplate.gradle`, `launcherTemplate.gradle`, `baseProjectTemplate.gradle` and their `.meta`s) are gitignored, so a fresh clone / CI / build machine relies on EDM regenerating them from the pinned packages (Assets → External Dependency Manager → Android Resolver → Force Resolve). Owner: AR-21 — when wiring the real Android build, decide whether to **commit** the resolved Gradle templates (reproducible, version-controlled build config — remove the matching `.gitignore` lines) or keep them generated-on-resolve; and confirm the resolved templates carry the AR/Play dependencies (ARCore, Play Billing) the build needs. Note: the active build target being Android is what triggers EDM auto-resolve, so this also recurs if a later story flips the build target — the committed folder prevents the crash either way. [.gitignore "Android Gradle templates generated by …" block; Assets/Plugins/Android/.gitkeep]

## Deferred from: code review of 1-3-persist-player-state-with-encrypted-atomic-local-save (2026-06-12)

- **`SampleScene` left enabled at build index 1** — Ships dead template content in the player build and is a second loadable scene that never runs the composition root (an accidental load of it in a player runs against an unwired locator). Spec-sanctioned for Story 1.3 ("SampleScene may stay if Bootstrap comes first"). Owner: **Story 8.2** (re-pointed 2026-06-16 from the now-closed Story 6.3 per the render/scene/device re-point header above) — remove SampleScene from the build list when the real scenes land. [ProjectSettings/EditorBuildSettings.asset]
- **`SaveService` event surface incomplete for Epic 6 UI binding** — (1) The fire-and-forget `InitializeAsync` kick in Bootstrap means a corrupt save raises `OnLoadFailed` (on a background thread) during scene-0 boot, before any UI exists to subscribe; the event has no replay-on-subscribe, so late binders must check `Status == Corrupt` first — either document that binding contract in the Epic 6 recovery dialog story or add replay-on-subscribe then. (2) AR-19's "start/finish events for long ops" intent isn't fully covered: no load-started/load-completed events, and `StartFreshAsync`'s delete+persist fires no save events. Owner: Epic 6 (recovery dialog + status UI). [Assets/Veilwalkers/Persistence/SaveService.cs; Assets/Veilwalkers/App/Bootstrap.cs]

## Deferred from: code review of 1-2-establish-assembly-boundaries-and-composition-root (2026-06-11)

- **Bootstrap runtime lifecycle not wired** — `Bootstrap` exists but is in no scene, so the composition root never executes at runtime; `GameServices.IsReady` stays false for any play session and the wire-once → read pattern is proven only by EditMode tests. When Bootstrap enters a scene with the first real service (Story 1.3), also add: an execution-order guarantee (`[DefaultExecutionOrder]` or `RuntimeInitializeOnLoadMethod` wiring) so other `Awake`s can't race it; a `RuntimeInitializeOnLoadMethod(SubsystemRegistration)` static reset for the domain-reload-disabled Editor option (`ProjectSettings/EditorSettings.asset` already stores `m_EnterPlayModeOptions: 3`, currently inert); and a recovery path for a mid-wiring `Register` failure (today the locator stays poisoned: not ready, but re-running wiring dies on duplicate registration). Owner: Story 1.3. [Assets/Veilwalkers/App/Bootstrap.cs]
- **[SETTLED in Story 3.5] `AnchorToken` has no validity contract** — Resolved 2026-06-14: added `AnchorToken.IsValid` (`!string.IsNullOrEmpty(trackableId)`) + `AnchorToken.None` (the explicit-identity-rotation "no anchor" sentinel, NOT `default` which has a zero-quaternion). The restore path (`AnchorRestoreService.TryRestore`) reads `IsValid` to treat a None/corrupt token as "no trackable to re-acquire" → it skips the native re-acquire and goes straight to relocation/Failed, never crashing (pinned by `Invalid_token_does_NOT_attempt_a_reacquire_...` asserting `ReacquireCalls == 0`, and `AnchorToken_validity_contract`). The type still ACCEPTS any value on load (DTO contract — the ctor does not throw); `IsValid` is the read-time guard. (Original note: `default(AnchorToken)` was indistinguishable from a real token.) [Assets/Veilwalkers/Core/Contracts/AnchorToken.cs (IsValid/None); Assets/Veilwalkers/AR/AnchorRestoreService.cs (consumes IsValid)]
- **EDM4U resolved artifacts not gitignored** — `Assets/Plugins/Android/*.aar|*.jar|*.srcaar` are not ignored; if EDM4U's "Patch mainTemplate.gradle" is off (its setting lives in a non-versioned location, so fresh machines can differ), the Android Resolver copies resolved binaries straight into this tracked folder and they'd silently land in the next commit. Decide commit-vs-ignore policy at AR-21 together with the existing EDM4U template-commit decision above. Owner: AR-21. [.gitignore]
